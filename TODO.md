# 다음에 할 일 (2026-08-29 갱신)

저장소: https://github.com/babodabin/casino · 배포: https://babodabin.github.io/casino
main에 푸시하면 GitHub Actions가 자동 배포합니다. **푸시 = 라이브 배포**입니다.

## 바로 시작할 것

이 순서로 하면 됩니다.

**8월 29일에 방향이 바뀌었습니다. 아래가 지금 기준입니다.**

1. **카드 게임을 한 화면에 고정** — 스크롤 없이 판 전체가 한눈에 들어오게 합니다. 자리가 모자라면 필요 없는 것을 뺍니다(차례 표시·더미 장수 줄은 빼도 됩니다). **블랙잭·고스톱·맞고는 끝났습니다.** 남은 것: 하이로우, 세븐 포커, 바카라, 파이 고우, 틴 파티, 비디오 포커, 섰다, 도리짓고땡, 홀덤·오마하
   - 마작 네 종류·차이니즈 포커·빅투는 패가 너무 많아 안 될 수 있습니다. 재 보고 안 되면 못 한다고 말씀드립니다
2. **1대 다수** — 손님은 배경 인형이 아니라 실제로 판단하는 상대여야 합니다. 고스톱·맞고는 이미 됩니다(`playerCount`). 남은 것은 바카라(제일 쉬움 · 카드 로직 안 건드림) → 블랙잭 → 파이 고우 → 홀덤·오마하(제일 큼 · 베팅 순서와 사이드 팟)
   - **반원형은 손님이 늘어 안 들어가면 포기하고 세로 배치로 바꿉니다.** 보기 편한 쪽이 우선입니다
3. **스크린낚시** — 행운의 물고기 자리. 던지기 → 입질 → 챔질 타이밍 → 릴 싸움
4. **배경 사진** — 첫 페이지는 건드릴 필요 없습니다. 슬롯·카지노 게임·카드 게임 뒤에 깔 사진을 정해야 하고, **파일을 `assets/`에 넣어 주셔야 합니다**
5. **움직임 다듬기** — 윷 던지는 느낌, 비디오포커 카드 회전, 피시 레이스 물고기 멈칫

### 끝난 것

- 경륜 원형 벨로드롬 · 홀덤·오마하 한 장씩 열기(이로써 한 장씩 열기 전부 완료)
- 반원형 딜러 테이블 — 블랙잭·바카라·파이 고우·틴 파티. 딜러가 있는 게임에는 슈·버림 통·칩 트레이를, 컴퓨터가 상대인 게임에는 상대 좌석과 카드 더미만 놓습니다
- 아이폰 아래 검은 띠 — 노치 여백을 배경 바깥에 줘서 생긴 것. 배경 안쪽으로 옮겼습니다(첫 화면도 같은 원인)
- 진 쪽 카드가 안 보이던 것 — `cardDim` 투명도 0.04 → 0.4

## 어디를 고치면 되는지

`App.tsx`가 6,400줄이라 매번 찾아 헤매면 시간이 많이 듭니다. 줄 번호는 금방 어긋나므로
**함수 이름과 스타일 이름**으로 적어 둡니다. 이 이름으로 검색하면 바로 나옵니다.

| 할 일 | 화면 함수 | 손댈 스타일 · 값 |
| --- | --- | --- |
| 1. 경륜 원형 트랙 | `CycleRacingGameScreen` | `cycleTrack` · `cycleLane` · `cycleRider` · `cycleJersey` |
| 2. 홀덤·오마하 나눠 열기 | `PokerGameScreen` (`mode`로 홀덤/오마하 겸용) | 안에 있는 `stage` 상태, `holdemCommunity` · `holdemCards` |
| 3. 테이블 모양 | 카드 게임 전반 | `holdemTable`(이미 둥근 테이블, 반경 110) · `feltLook`(공용 펠트) · `cardRow` |
| 4. 카드 크기 | `PlayingCard`(공용 카드 컴포넌트) | `playingCard` 72×108 · `compactPlayingCard` 58×88 · `hwatuCard` 52×78(**건드리지 말 것**) · `tujeonCard` 52×96 |
| 5. 스크린낚시 | 새로 만들기 | 아래 "새 게임 붙이는 자리" 참고 |
| 6. 배경 사진 | 화면별 바탕 | `app`(앱 전체) · `detailScreen` · `pokerTable` `#052E22` · `baccaratScreen` `#071D25` |
| 7. 윷 던지는 느낌 | `YutBetGameScreen`, `YutStickView` | `yutStick` · `yutStickTumbling` · `yutMat` |
| 7. 비디오포커 회전 | `VideoPokerGameScreen` | `videoPokerHand` · `videoPokerCardWrap` |
| 7. 피시 레이스 멈칫 | `FishRaceGameScreen` | `fishSwim` · `fishLane` · `fishRunner`, 사건은 `src/fishrace.ts`의 `events` |

⚠️ **카드 크기는 값만 줄이면 안 됩니다.** `playingCard`와 `compactPlayingCard`는 **공용**이라
블랙잭 · 바카라 · 홀덤 · 오마하 · 세븐 포커 · 하이로우 · 파이 고우 · 틴 파티 · 비디오 포커 ·
빅투 · 차이니즈 포커 · 조커 포커가 전부 같이 바뀝니다. 카드가 작아지면 그 줄들의 높이와
간격(`cardRow`의 `minHeight: 126` 등)도 같이 손봐야 하고, 안 그러면 카드 아래에 빈 공간이 남습니다.
줄인 뒤 이 화면들을 한 번씩 열어 확인해야 합니다.

**한 장씩 공개를 다른 게임에 더 넣을 때** — 공용 도구가 이미 있습니다.
`useReveal()`(연 장수 세기)와 `<RevealButton opened total onPress label/>`. 쓰는 방법은
`BaccaratGameScreen`이나 `TujeonGameScreen`을 그대로 보고 따라 하면 됩니다.
핵심은 **정산(`onSettle`)을 마지막 장이 열린 뒤로 미루는 것**입니다.

### 새 게임 붙이는 자리 (다섯 군데를 다 고쳐야 합니다)

1. `src/<이름>.ts` — 판정 로직 (반드시 `.ts`, `.tsx` 아님)
2. `tests/<이름>.test.ts` + `package.json`의 `test` 스크립트에 파일 이름 추가
3. `App.tsx`의 `gameCategories` — 목록에 한 줄
4. `App.tsx`의 `AppScreen` 타입, `gameEntryScreens`, `settleNewGame`의 게임 이름 목록
5. `App.tsx`에 `<이름>SetupScreen`(대개 `SimpleSetupScreen` 재사용)과 `<이름>GameScreen` 렌더 줄

## 지금 상태

- 게임 **34개**, 전부 플레이 가능
- 테스트 **571개 통과**, 타입 오류 0, 웹 빌드 확인됨
- 어디까지 했는지는 `git log --oneline -5`로 봅니다. (커밋 해시를 여기 적으면 이 문서를 커밋하는 순간 바로 틀려집니다.)

| 카테고리 | 게임 |
| --- | --- |
| 한국 전통 | 고스톱 · 맞고 · 섰다 · 도리짓고땡 · 투전 · 윷 베팅 |
| 카지노 | 블랙잭 · 바카라 · 룰렛 · 크랩스 · 식보 · 슬롯 |
| 포커·카드 | 텍사스 홀덤 · 오마하 · 세븐 포커 · 차이니즈 포커 · 비디오 포커 · 하이로우 |
| 마작 | 리치 · 중국식 · 홍콩 · 사천 (4개, 그대로 두기로 함) |
| 스포츠/아케이드 | 경마 · 경륜 · 예측 마켓 · 그레이하운드 · 피시 레이스 · 행운의 물고기 |
| 세계 게임 | 야찌 · 파이 고우 · 틴 파티 · 빅투 · 조커 포커 · 공 어디에? |

## 8월 28~29일에 한 것

**새 게임 다섯** — 투전(한국 전통) · 차이니즈 포커(포커) · 빅투(세계) · 조커 포커(세계) · 예측 마켓(스포츠/아케이드).

**한 장씩 공개** — 베팅을 누르면 바로 결과가 나오던 것을 열 게임에서 고쳤습니다.
바카라 · 블랙잭 · 섰다 · 도리짓고땡 · 투전 · 틴 파티 · 파이 고우 · 세븐 포커 · 하이로우 · 차이니즈 포커.
공통 규칙: 여는 도중에는 점수·족보·강조·승패가 새지 않고, **정산도 마지막 장이 열린 뒤**에 합니다.
공용 도구는 `useReveal`과 `RevealButton`입니다.

**고친 버그**
- 탭바가 마지막 줄을 가리던 것 (`Page`가 탭바 높이만큼 아래를 비웁니다)
- 화투 그림 안에 흰 여백이 박혀 있던 것 (48장 다시 잘라냄, `scripts/trim-hwatu.mjs`)
- 경마 말과 피시 레이스 물고기가 거꾸로 달리던 것
- 잔액과 BET 글자가 겹치던 화면 다섯 개

**디자인** — 카드가 놓이는 자리에 초록 펠트를 깔았습니다(`feltLook`). 화투·투전은 실제로 펠트를 안 쓰므로 뺐습니다. 색은 원래 있던 홀덤 테이블 색을 그대로 씁니다.

**접은 것** — 코인 푸셔. 두 번 다시 만들었지만 마음에 들지 않아 그레이하운드를 되돌렸습니다. 코드는 남아 있습니다.

## 아직 답이 안 난 것

- **"중간에 그은 줄"** — 바카라 판 가운데 선을 없앴는데 그것이 맞는지 확인이 필요합니다. 다른 줄이면 어느 게임인지 알려 주셔야 합니다

## 미해결

**아이폰에서 하단 바가 다시 올라옴** — 전체화면으로 쓰다가 다른 앱을 보고 돌아오면 탭바 아래에 빈 띠가 생깁니다. 지금 처리는 여백을 없앤 게 아니라 **같은 색으로 가린 것**입니다(`public/index.html`의 배경색 + 배포 워크플로의 문자열 치환). 실기기에서만 재현되어 여기서 검증할 수 없습니다.

## 작업 규칙

- **모르면 묻습니다. 정해 준 범위 밖으로 나가지 않습니다.** 여러 대상을 말했을 때 어느 것인지 확인하고 시작합니다
- 카테고리마다 게임 6개를 넘지 않습니다
- **목록에서 뺀 게임의 코드는 지우지 않습니다.** `gameCategories`에서만 뺍니다. 한 줄 넣으면 되살아납니다
- 판정 로직은 반드시 `src/*.ts`에 둡니다. 테스트는 `.ts`만 읽을 수 있어 JSX가 든 `.tsx`는 못 읽습니다
- 새 게임을 만들면 `package.json`의 `test` 스크립트에 테스트 파일을 **직접 추가**해야 합니다
- **목록에만 넣고 화면을 안 만들면 시작할 때 오류 화면이 뜹니다**
- 배포 워크플로가 `App.tsx`에서 탭바 한 줄을 문자열로 정확히 1개 찾아 바꿉니다. **못 찾으면 빌드가 실패합니다**
- 다 하지 않고 "완성했다"고 말하지 말 것

### 이 PC에서 작업할 때

- Node는 winget으로 설치한 24.19.0인데 셸 PATH에 안 잡힙니다. 명령 앞에 `export PATH="/c/Program Files/nodejs:$PATH"`를 붙이세요
- `node_modules`는 pnpm 설치본입니다. `npm install`은 이 저장소에서 실패하니, 도구가 필요하면 저장소 밖 폴더에 따로 설치해 경로로 부르세요(`scripts/trim-hwatu.mjs`가 그 방식입니다)
- **커밋 전에 `git fetch origin`으로 원격을 확인하세요.** GitHub 웹에서 직접 올리는 경우가 있습니다

## 문제 데이터 갱신 (예측 마켓)

`.github/workflows/fetch-kalshi.yml`이 매일 한국 시간 아침 6시에 kalshi.com에서 받아 `src/predictdata.ts`로 커밋합니다. 수동 실행도 됩니다. 앱은 그 파일만 읽습니다(kalshi가 CORS를 안 열어 브라우저에서 직접 못 부릅니다).

## 목록에서 빠졌지만 코드가 살아 있는 게임

한 줄만 넣으면 되살아납니다.

| 게임 | 코드 |
| --- | --- |
| 육백 | `src/yukbaek.ts` |
| 자동차 레이스 | `src/carracing.ts` |
| 소싸움 | `src/bullfighting.ts` |
| 홀짝 | `oddEvenSetup`/`oddEvenGame` |
| 파이브 카드 드로우 | `src/fivecarddraw.ts` |
| 민화투 | `src/minhwatu.ts` |
| 로또 | `src/worldgames.ts` |
| 즉석 복권 | `ScratchGameScreen` |
| 경정 | `src/boatracing.ts` |
| 코인 푸셔 | `src/coinpusher.ts` |

## 손 안 댄 것

**`README.md`가 오래됐습니다.** 한국 전통이 "준비 중"이라고 되어 있고, 목록에서 빠진 게임이 그대로 있습니다. 게임 작업이 끝난 뒤 한 번에 고치는 편이 낫습니다.
