# 다음에 할 일 — 9월 1일 (2026-08-31 밤에 적음)

저장소: https://github.com/babodabin/casino · 배포: https://babodabin.github.io/casino
main에 푸시하면 GitHub Actions가 자동 배포합니다. **푸시 = 라이브 배포**입니다.

**이 문서 하나만 읽으면 바로 시작할 수 있게 적었습니다.** 코드를 뒤져 상황을 파악할 필요가
없도록 어디를 고치는지와 왜 그렇게 되어 있는지를 같이 적어 두었습니다.

## 지금 상태

- 게임 **32개**, 전부 플레이 가능 · 테스트 **654개 통과** · 타입 오류 0
- 마지막 배포: `c309bb5` (2026-08-31 밤) — 분류 순서와 받기 한 걸음 없앤 것까지 라이브입니다
- 어디까지 했는지는 `git log --oneline -8`로 봅니다

**분류 순서는 화면에 보이는 순서 그대로입니다.** 바꾸려면 `gameCategories` 배열 순서만 바꾸면 됩니다.

| 분류 | 게임 | 개수 |
| --- | --- | --- |
| 한국 전통 | 고스톱 · 맞고 · 섰다 · 도리짓고땡 · 투전 · 윷 베팅 | 6 (꽉) |
| 마작 | 리치 · 중국식 · 홍콩 · 사천 | 4 |
| 카드 | 텍사스 홀덤 · 오마하 · 세븐 포커 · 하이로우 · 차이니즈 포커 · 빅투 | 6 (꽉) |
| 딜러 | 블랙잭 · 바카라 · 파이 고우 · 틴 파티 · 조커 포커 | 5 |
| 주사위 | 야찌 · 크랩스 · 식보 · 룰렛 · 물고기 룰렛 | 5 |
| 자동 배팅 | 슬롯 · 경마 · 경륜 · 예측 마켓(스포츠) · 예측 마켓(사회문제) · 비디오 포커 | 6 (꽉) |

---

# 다음에 이것부터

**1번(파이 고우)부터 시작하면 됩니다.** 제일 작고 티가 제일 빨리 납니다.
아래 순서는 크기와 막히는 것을 보고 정했습니다. 바꿔도 됩니다.

| 순서 | 할 일 | 크기 | 막는 것 |
| --- | --- | --- | --- |
| **1** | **파이 고우 — 내 패를 하이 5장 / 로우 2장 두 줄로** | 작음 | 없음 |
| 2 | **껍데기 다섯 화면** — 홈 · 게임 목록 · 지갑 · 기록 · 설정 | 큼 | 없음 (그림 없이도 배치·색은 됩니다) |
| 3 | **발라트로 하드** | 큼 | 없음. **배당을 시뮬레이션으로 다시 재야** 합니다 |
| 4 | 카드 크기 — 차이니즈 포커 · 마작 4종 | 중간 | 없음 (아래 4번에 잰 값이 있습니다) |
| 5 | 고스톱 모은 패 자리 | 작음 | **"공부해와"** — 실제 고스톱 앱을 보고 오라 하셨습니다 |
| 6 | 배경 사진 · 게임 그림 · 윷 던지는 느낌 · README | 작음 | **그림 파일을 주셔야** 시작할 수 있습니다 |

## 디자인 — 정해진 것과 안 정해진 것

**정한 것**

- 반원 딜러 테이블은 **포기**합니다. 카드와 글자가 안 잘리는 쪽이 먼저입니다
- 색은 **자주 도는 검정 + 금색 세 단**. 게임 판 안까지 같은 결로 갑니다(첫 화면과 안 어긋나게)
- **화려함은 자리를 안 먹는 것부터** — 비네트 · 금색 발광 · 두 색 테두리 · 움직임.
  그림자·두꺼운 테두리·큰 글씨는 높이를 늘려 잘림을 되살립니다
- 발라트로는 **이지(지금 것 그대로) / 하드(길게)** 둘. 목록 자리는 하나만 씁니다
- 발라트로 하드 상점은 **진짜 WC**로 삽니다

**아직 안 정한 것**

- 목업의 **충전 · 출금**(실제 결제)과 **계정 · 회원 ID · 고객센터 · 약관**(서버 필요)을
  껍데기만 둘지, 지금 구조(게스트 · 가상 코인)에 맞게 아예 뺄지
- 배경 사진과 게임 그림을 어떻게 구할지. **보내주신 목업 다섯 장은 292×831이라
  배경으로는 못 씁니다**(흐려집니다) — 참고용으로만 씁니다

⚠️ 발라트로 하드는 상점 때문에 **한 판에 거는 돈이 도중에 늘어납니다.** 지금 배당표가
전부 안 맞게 되므로 **시뮬레이션으로 다시 재서** 환급률 95%에 맞춰야 합니다.

---

## 1. 파이 고우 — 내 패를 두 줄로 (제일 작고 티가 납니다)

**지금**: 내 일곱 장이 한 줄입니다. 폭 301에 일곱 장을 넣느라 카드가 `mid` 58×88까지
내려갑니다. 72×108로는 폭이 366이라 안 들어갑니다.

**할 것**: 내 패를 **하이 5장 / 로우 2장 두 줄**로 나눕니다. 그러면 한 줄에 다섯 장뿐이라
카드가 `big` 72×108로 올라갑니다.

- 다섯 장 줄: 72 × (1 + 4 × 0.68) = **268** ≤ 301 ✓
- 두 장 줄: 안 겹치고 나란히 놓아도 72 × 2 + 6 = **150** ✓
- 로우로 고른 두 장이 **아래 줄로 내려가게** 만들면 규칙(하이 5 · 로우 2)도 같이 가르쳐 줍니다

⚠️ **줄이 셋이 됩니다**(딜러 7 · 내 하이 5 · 내 로우 2). `useCardFit`의 `rows`를 3으로 바꾸고
**`spare`를 다시 재야 합니다.** 지금 394는 줄이 둘일 때 잰 값입니다.

⚠️ 딜러 일곱 장을 그대로 한 줄(58×88)로 둘지, 승부 뒤에 같이 5+2로 나눌지 정해야 합니다.
딜러도 나누면 줄이 넷이 되어 높이가 빠듯해집니다 — **딜러는 한 줄로 두는 쪽을 권합니다.**

고칠 곳: `PaiGowGameScreen`의 `cardRow` · `fan` · `useCardFit({rows:2,spare:394,...})`.

---

## 2. 껍데기 다섯 화면 — 목업대로 배치

`assets/`에 올려 주신 목업 다섯 장이 이 다섯 화면입니다. **그림은 아직 없어도** 배치와
색은 다 됩니다. 그림 자리는 지금처럼 글자 아이콘(♠·魚)이 들어갑니다.

| 화면 | 할 것 |
| --- | --- |
| 위 바 | 왼쪽 프로필 원 · 가운데 **금테 알약 안에 코인** · 오른쪽 원형 아이콘 |
| 아래 바 | 다섯 칸에 **가운데만 원으로 튀어나온 왕관** |
| 홈 | 사진 배경 자리 · 큰 제목 · `PLAY NOW` 큰 금색 버튼 · 아래 작은 버튼 하나 |
| 게임 목록 | **2열 세로 카드 그리드**(지금은 가로로 긴 줄). 카드마다 그림 칸 + 이름 |
| 기록 | 위에 **3칸 요약 박스**(총 플레이 · 총 승리 · 최대 승리) · 아래 줄마다 썸네일 + 이름/날짜 + 오른쪽 초록/빨강 금액 |
| 지갑 | 큰 잔액 카드 · 아래 네 칸 버튼 |
| 설정 | 프로필 카드 · 줄마다 아이콘 + 이름 + `›` · 스위치 |

⚠️ **화려함은 높이를 안 먹는 것부터.** 그림자·두꺼운 테두리·큰 글씨는 높이를 늘려
겨우 맞춰 놓은 게임 판을 다시 넘치게 합니다.

**아직 안 정한 것** — 목업의 `충전`·`출금`(실제 결제)과 `계정 정보`·`보안 설정`·`고객센터`·
`이용 약관`(서버 필요)을 **껍데기만 둘지, 아예 뺄지**. 지금 앱은 게스트 · 가상 코인뿐이라
눌러도 아무 일이 없습니다.

---

## 3. 발라트로 하드

**목록 자리는 하나만 씁니다.** `발라트로` 한 줄만 두고 **들어가서 이지/하드를 고릅니다** —
슬롯이 클래식/파치슬롯을 고르는 방식과 같습니다. (딜러가 5개라 자리는 남지만, 같은 게임을
두 줄로 넣지 않습니다.)

**이지 = 지금 조커 포커 그대로.** 여덟 장 · 조커 3장 · 낼 기회 3 · 버릴 기회 2 ·
목표 2,650점 · 한 판 1~2분. **손대지 않습니다.**

**하드 — 넣을 것 다섯**

| | 넣을 것 | 메모 |
| --- | --- | --- |
| 1 | **블라인드 3단** — 작은 · 큰 · 보스 | 셋을 다 깨야 정산. 한 판 5~8분 |
| 2 | **보스 조건** | "♠는 점수 안 남" · "첫 두 장은 버려짐" 같은 것을 여러 개 만들어 무작위로 |
| 3 | **족보 레벨** | 같은 족보를 쓸수록 그 족보의 칩과 배수가 오릅니다 |
| 4 | **라운드 사이 상점** | 조커를 사고, 조커 칸을 3개 → **5개**로 |
| 5 | **칩 × 배수를 눈에 보이게** | 지금은 최종 점수만 나옵니다 |

**안 넣습니다** — 타로 카드 · 바우처 · 덱 종류. 진짜 발라트로만큼은 안 갑니다.

⚠️ **상점에서 진짜 WC를 씁니다.** 그래서 **한 판에 거는 돈이 도중에 늘어납니다.**
조커를 많이 사면 이길 확률은 오르지만 지면 산 돈까지 다 잃습니다.
**지금 배당표(`jokerLadder` 1.7 · 3 · 10배)가 전부 안 맞게 됩니다** —
작업 규칙대로 **시뮬레이션으로 다시 재서 환급률 95%에 맞춰야** 합니다.

고칠 곳: 로직 `src/jokerpoker.ts`(또는 `src/balatro.ts`를 새로 만들기), 화면
`JokerPokerGameScreen`. **새 파일을 만들면 `package.json`의 `test` 스크립트에 테스트
파일 이름을 직접 넣어야 합니다.**

---

## 4. 카드 크기 — 남은 화면 (차이니즈 포커 · 마작)

**도구는 다 만들었습니다**(`useCardFit`, 8월 31일 밤). 파이 고우와 블랙잭에 붙였고
남은 것은 아래 둘뿐입니다. 붙이는 법은 그 두 화면을 그대로 따라 하면 됩니다.

**실제로 재 본 넘침**(아이폰 375×812, 화면 높이 764)

| 화면 | 넘침 | 어떻게 |
| --- | --- | --- |
| 차이니즈 포커 | **764 / 1379 — 615 넘침** | 카드만 줄여서는 300쯤 모자랍니다. 손패 13장을 부채로 겹치고 세 줄 칸을 줄여야 합니다 |
| 리치 마작 (4종) | **764 / 1368 — 604 넘침** | 넘치는 것의 절반이 카드가 아니라 **설명 패널**입니다(초보 진행 도움). 접을 수 있게 하는 것이 먼저입니다 |
| 빅투 | 안 넘침 | 이제 들어갑니다 |
| 조커 포커 | 안 넘침 | 이제 들어갑니다 |

- **마작·고스톱처럼 패가 많아 작게 넣은 것은 오히려 조금 크게** 합니다.
  작아서 안 보이는 게 더 문제입니다
- 카드 크기는 게임마다 다르게 하지 말고 `CardSize` 네 단계만 씁니다

⚠️ **왜 중요한가** — 지금까지 실제로 터진 문제는 **전부 "자리가 모자람"** 이었습니다.
파이 고우는 카드가 두 줄로 접혀 버튼이 화면 밖으로 밀렸고, 블랙잭은 결과가 나오면
`같은 금액으로 다시 하기`가 잘렸습니다. **"남을 때 어떻게 쓸까"는 드문 쪽입니다.**

**같이 볼 것** — **위 자리 카드가 아직 12만 겹치는 것**(`tableTopCardOverlap`).
이제 `cardFanSpread` 0.68이 기준이니 그 값에 맞추면 됩니다.

## 5. 고스톱 — 남은 것

- **모은 패 무리 자리가 아직 흔들립니다.** 광·열끗·띠·피 네 무리로 나눠 놓았는데 어느
  무리가 비면 그만큼 자리가 당겨집니다. **비어도 자리를 비워 두어야** 합니다
- **판 가운데가 넓게 빕니다**(`goStopFloorArea`가 남는 자리를 나눠 가짐)
- ⚠️ **"공부해와"** — 실제 고스톱 앱들이 모은 패를 어떻게 늘어놓는지 먼저 보고 오라는 뜻입니다

## 6. 그 밖에 남은 것

- **배경 사진** — 첫 페이지는 그대로 두면 됩니다. 슬롯·카지노·카드 게임 뒤에 깔 사진을
  정해야 하고 **파일을 `assets/`에 넣어 주셔야 합니다**(대화에 올린 사진은 저장소에 못 넣습니다)
- **윷·주사위 던지는 느낌** — 지금은 **던지는 동작이 없습니다.** 버튼을 누르면 결과가
  나오고 윷은 도는 동안 흐려지기만 합니다. 화면을 쓸어 던지는 조작(`PanResponder`)은
  즉석 복권 긁기에만 붙어 있습니다
- **README가 오래됐습니다.** 한국 전통이 "준비 중"으로 되어 있고 목록에서 빠진 게임이
  그대로 있습니다. 게임 작업이 끝난 뒤 한 번에 고치는 편이 낫습니다
- **1대 다수 — 딜러 분류**: 바카라(제일 쉬움) → 블랙잭 → 파이 고우 순으로 손님을 여럿
  앉히는 것. 반원형 `DealerTable`은 안에 넣은 것을 세로로 쌓기만 하므로 손님을 더 넣으면
  원 둘레에 앉는 게 아니라 줄만 늘어납니다. 로직도 `dealInitialRound`가 `player` 하나만,
  `dealPaiGow`가 두 몫만 뗍니다. **바카라만 예외로 쉽습니다** — 손님이 몇이든
  PLAYER·BANKER 한 벌을 같이 보므로 카드 로직은 그대로 두고 베팅만 늘리면 됩니다

## 미해결 (여기서 확인 불가)

**아이폰에서 하단 바가 다시 올라옴** — 전체화면으로 쓰다가 다른 앱을 보고 돌아오면 탭바
아래에 빈 띠가 생깁니다. 지금 처리는 여백을 없앤 게 아니라 **같은 색으로 가린 것**입니다
(`public/index.html`의 배경색). 실기기에서만 재현되어 여기서 검증할 수 없습니다.

---

# 어디를 고치면 되는지

`App.tsx`가 7,600줄이 넘어 매번 찾아 헤매면 시간이 많이 듭니다. 줄 번호는 금방 어긋나므로
**함수 이름과 스타일 이름**으로 적어 둡니다. 이 이름으로 검색하면 바로 나옵니다.

| 할 일 | 화면 함수 | 손댈 이름 |
| --- | --- | --- |
| 물고기 룰렛 | `FishRouletteGameScreen` | 보이는 원은 `fishRouletteWaterSize` 248 하나뿐 · 자리는 그 바깥 `fishRouletteRing` 140 · 물고기가 서는 곳 `fishRouletteFishRing` 118 · `fishRouletteBoard` 320은 **안 보이는 틀** · 문어는 `fishRouletteOctopusSize` 34 · 로직은 `src/fishroulette.ts` |
| 룰렛 휠 | `RouletteGameScreen` | 도는 것은 `spin`(감속 곡선 하나) · `rouletteWheel` 316 · `rouletteWheelRing` 300 · `rouletteBowl` 150 · `rouletteHub` 22 |
| 카드 게임 자리 | 세 화면의 `seatRow`(`SevenPoker` · `HighLow` · `Poker`) | 아래 "카드 화면에서 고정해 둔 값" |
| 카드 크기 | `PlayingCard`(공용) | `playingCard` 72×108 · `compactPlayingCard` 58×88 · `tinyPlayingCard` 46×70 · `stacked`(모서리 표시) |
| 카드 깔리는 속도 | `useTableDeal`(공용) | `useReveal` 바로 옆 · `DEAL_MINE_MS` 200 · `DEAL_THEIRS_MS` 100 · `DEAL_BUDGET_MS` 1600 |
| 인원 고르기 | `PlayerCountRow` · 상태는 `tablePlayers` | 베팅 한 라운드는 `src/table.ts`의 `applyTableAction` |
| 파치슬롯 | `PachislotGameScreen` | 로직 `src/pachislot.ts`(천장 `pachiCeiling` · 찬스존 `pachiZoneEvery` · 환급률은 `pachiAtContinue`로 맞춤) |
| 예측 마켓 | `PredictGameScreen`(`group`으로 갈래) | 로직 `src/predict.ts`의 `predictGroupOf` · 문제는 `src/predictdata.ts` |
| 고스톱 | `GoStopGameScreen` | 치는 모습은 `slap`/`slapPop` · 모은 패는 `takenRow` · `goStopTakenRow` · 로직 `src/gostop.ts` |
| 화투패 크기 | `HwatuCardView`(`size`로 고름) | `hwatuCard` 52×78 · `hwatuCardSmall` 40×60 · `hwatuCardTiny` 20×30 |
| 블랙잭 | `BlackjackGameScreen` | 딜러가 스스로 여는 간격 `DEALER_REVEAL_MS` 620 |
| 비디오 포커 | `VideoPokerGameScreen` | 카드 도는 시간 `VIDEO_POKER_FLIP_MS` 820 · 배당표 `videoPokerPaytable` · 번쩍이는 카드는 `videoPokerMadeCards`(`src/videopoker.ts`) · 튀는 코인 `videoPokerCoinLayer` |
| 경륜 | `Velodrome` | `velodromeSize` · `velodromeRiderText` |
| 색 한 벌 | `colors` 상수 하나 | 여기만 고치면 32개 화면이 같이 바뀝니다 · 금색은 `goldDeep`/`gold`/`goldLight` 세 단 |
| 글씨체 | `public/index.html`의 `<style>` | `html body #root *`가 본문 · `[data-display]`가 제목 세리프 · App.tsx의 `displayFont` |
| 방 조명 | `roomLight` · `roomVignette` | 앱 맨 바깥. 그림자로만 그려 자리를 안 먹습니다 |
| 배경 사진 | 화면별 바탕 | `app` · `detailScreen` · `pokerTable` `#052E22` · `baccaratScreen` `#071D25` · `goStopBoard` `#0E5636` |
| 윷 던지는 느낌 | `YutBetGameScreen` · `YutStickView` | `yutStick` · `yutStickTumbling` · `yutMat` |

## 카드 화면에서 고정해 둔 값 (건드리면 판이 흔들립니다)

세븐 포커 · 하이로우 · 홀덤 · 오마하 네 화면은 **판이 늘었다 줄었다 하지 않도록** 자리마다
높이를 미리 잡아 두었습니다. 카드 크기나 줄을 바꿀 때는 이 값도 같이 봐야 합니다.

| 이름 | 값 | 왜 |
| --- | --- | --- |
| `tableSideSlotTall` | 268 | 좌우 자리에 일곱 장이 다 와도 안 늘어나게 |
| `tableMyCardsSeven` | 278 | 내 카드 줄 폭 고정. 가운데 정렬이면 장수가 늘 때마다 왼쪽으로 밀림 |
| `tableFrontRow` | 56 | 판 가운데 앞줄(내 차례엔 버튼, 아니면 방금 일어난 일) |
| `tableBottomSlot` | 70 | 아래 버튼 자리. **큰 버튼이 70인데 안내 글은 52라 한 수마다 18씩 오르내렸습니다** |
| `tableLegendSlot` | 30 | 맨 아래 설명 줄(한 줄·두 줄을 오감) |
| `tableOutcomeSlot` | 24 (하이로우 48) | 결과 줄 |
| `tableSeatStatusSlot` | 15 | 폴드 · 승리 · 차례 표시 |
| `tableSeatCards` | minHeight 82 | 폴드한 사람이 카드를 안 열 때 자리가 쪼그라들지 않게 |
| `tableChipPile` | 29 | 칩이 한 개에서 다섯 개가 돼도 자리가 안 커지게 |
| `holdemTable` 모서리 | 64 | **110이면 판 맨 윗줄 폭이 127뿐이라 이름과 카드가 초록 판 밖으로 나갑니다** |
| `tableTopHead` | 220 | 위 자리 이름줄 폭. 둥근 모서리 안에 들어오게 |
| 겹치는 정도 | `tableCardStackDown` −52 · `tableTopCardOverlap` −12 · `sevenPokerCardOverlap` −26 · `pokerBoardFan` −27 | 좌우는 22, 위는 38, 내 카드는 36만 보이게 |

**잰 값**: 세븐 포커 4명 판에서 카드판 높이가 처음부터 승부까지 646으로 같고, 내 자리
위치도 517로 같습니다(승부 때만 6). 홀덤·오마하는 475로 처음부터 끝까지 같습니다.

## 한 화면에 고정하는 법

바깥을 `styles.fixedTableArea`(`flex: 1` · `space-between`)로 감쌉니다. 판에 원래 걸려
있던 `minHeight`(510 · 520 · 570)를 풀어야 아래가 안 밀립니다. 패가 많으면 부채처럼
겹칩니다. **이긴 카드는 위로 16 들리므로** 카드 줄에 `paddingTop`을 줘야 이름줄을 안 덮습니다.

자리가 모자랄 때 무엇이 먼저 양보하는지 정해 둬야 합니다. 지금은 반원 테이블이
`flexShrink: 1` + `overflow: hidden`이라 테이블 아래가 잘리고 버튼은 남습니다.
**버튼이 밀려 화면 밖으로 나가는 것보다 이쪽이 낫습니다.**

## 새 게임 붙이는 자리 (다섯 군데를 다 고쳐야 합니다)

1. `src/<이름>.ts` — 판정 로직 (반드시 `.ts`, `.tsx` 아님)
2. `tests/<이름>.test.ts` + `package.json`의 `test` 스크립트에 파일 이름 추가
3. `App.tsx`의 `gameCategories` — 목록에 한 줄
4. `App.tsx`의 `AppScreen` 타입, `gameEntryScreens`, `settleNewGame`의 게임 이름 목록
5. `App.tsx`에 `<이름>SetupScreen`(대개 `SimpleSetupScreen` 재사용)과 `<이름>GameScreen` 렌더 줄

---

# 작업 규칙

- **모르면 묻습니다. 정해 준 범위 밖으로 나가지 않습니다.** 여러 대상을 말했을 때
  어느 것인지 확인하고 시작합니다
- 카테고리마다 게임 6개를 넘지 않습니다
- **목록에서 뺀 게임의 코드는 지우지 않습니다.** `gameCategories`에서만 뺍니다
- 판정 로직은 반드시 `src/*.ts`에 둡니다. 테스트는 `.ts`만 읽을 수 있어 JSX가 든 `.tsx`는 못 읽습니다
- 새 게임을 만들면 `package.json`의 `test` 스크립트에 테스트 파일을 **직접 추가**해야 합니다
- **목록에만 넣고 화면을 안 만들면 시작할 때 오류 화면이 뜹니다**
- 배당은 **짐작하지 말고 시뮬레이션으로 재서** 정합니다. 잰 값은 코드 주석에 남깁니다
- **이겼는데 손해 보는 배당을 두지 않습니다**(1배 미만). 예측 마켓에서 한 번 터진 적 있습니다
- 다 하지 않고 "완성했다"고 말하지 말 것

## 이 PC에서 작업할 때

- Node는 winget으로 넣은 24.19.0인데 셸 PATH에 안 잡힙니다.
  명령 앞에 `export PATH="/c/Program Files/nodejs:$PATH"`를 붙이세요
- `node_modules`는 pnpm 설치본입니다. `npm install`은 이 저장소에서 실패하니, 도구가
  필요하면 저장소 밖 폴더에 따로 설치해 경로로 부르세요
- **커밋 전에 `git fetch origin`으로 원격을 확인하세요.** GitHub 웹에서 직접 올리는 경우가 있습니다
- 여러 줄을 고칠 때는 스크래치 폴더에 `.mjs`를 쓰고 `node`로 돌리는 편이 안전합니다.
  ⚠️ **`TODO.md`는 줄 끝이 CRLF**라 문자열로 찾을 때는 `\r\n`을 먼저 `\n`으로 바꿔야 맞습니다
- 화면 확인은 `.claude/launch.json`의 `casino-web`(expo · 8081)을 띄우고 **375×812**로 잽니다.
  고친 것이 화면에 안 나오면 새로고침하세요 — 스타일만 바뀔 때 가끔 안 붙습니다

## 화면 판번호 — "안 바뀐 것 같다"는 말이 나오면 이것부터

설정 맨 아래에 배포 날짜와 커밋이 찍힙니다(`EXPO_PUBLIC_BUILD`). 아이폰 홈 화면 앱이 옛
파일을 오래 들고 있어서 **몇 번이나 엉뚱한 곳을 고쳤습니다.** 판번호가 다르면 폰이 옛
화면을 들고 있는 것이고, 같은데도 안 바뀌었으면 그때가 진짜 버그입니다.

새로 받는 법: 홈 화면 앱을 완전히 닫았다 열기 → 안 되면 사파리에서 새로고침 →
그래도 안 되면 아이콘 지우고 다시 추가.

## 문제 데이터 갱신 (예측 마켓)

`.github/workflows/fetch-kalshi.yml`이 매일 한국 시간 아침 6시에 kalshi.com에서 받아
`src/predictdata.ts`로 커밋합니다. 수동 실행도 됩니다. 앱은 그 파일만 읽습니다
(kalshi가 CORS를 안 열어 브라우저에서 직접 못 부릅니다). 2026-08-30 기준 86개입니다.

## 목록에서 빠졌지만 코드가 살아 있는 게임

`gameCategories`에 한 줄만 넣으면 되살아납니다.

| 게임 | 코드 |
| --- | --- |
| 그레이하운드 | `src/greyhound.ts` · `GreyhoundRacingGameScreen` |
| 스크린낚시 | `src/screenfishing.ts` · `ScreenFishingGameScreen` |
| 피시 레이스 | `src/fishrace.ts` · `FishRaceGameScreen` |
| 공 어디에? | `src/shellgame.ts` · `ShellGameScreen` |
| 행운의 물고기 | `src/luckyfish.ts` |
| 코인 푸셔 | `src/coinpusher.ts` |
| 파이브 카드 드로우 | `src/fivecarddraw.ts` |
| 육백 · 민화투 | `src/yukbaek.ts` · `src/minhwatu.ts` |
| 자동차 레이스 · 소싸움 · 경정 | `src/carracing.ts` · `src/bullfighting.ts` · `src/boatracing.ts` |
| 로또 · 즉석 복권 · 홀짝 | `src/worldgames.ts` · `ScratchGameScreen` · `oddEvenSetup`/`oddEvenGame` |

**빈 칸은 주사위 1 · 자동 배팅 1 · 마작 2입니다.** 마작 칸은 마작이 아니면 못 들어갑니다.
새 카드 게임이나 새 배팅 게임을 넣으려면 그때 정해야 합니다(6개 규칙을 늘리거나 · 분류를
하나 더 만들거나 · 있는 것 하나를 빼거나).

---

# 지금까지 한 것

## 2026-08-31 (밤) — 색 한 벌 갈아엎기 · 글씨체 · 카드 잘림

**왜 카지노처럼 안 보였는가** — 색이 전부 **파란 회색**이었습니다.
`bg #080B12 · panel #111722 · border #293140`. 이건 업무용 대시보드 색입니다.
카지노 실내는 파란 어둠이 아니라 **자주빛 도는 따뜻한 어둠**입니다.

| 이름 | 전 | 후 |
| --- | --- | --- |
| `bg` | `#080B12` | **`#150E16`** |
| `panel` | `#111722` | `#221527` |
| `panel2` | `#151D2A` | `#2A1A2E` |
| `border` | `#293140` | `#3B2839` |
| 금색 | `#D1A63C` **하나** | **세 단** `goldDeep #6B4A16` · `gold #C9971F` · `goldLight #F5DE8A` |
| 새로 | — | `plum #5B2A6B` (은은한 자주 조명) |
| 초록 펠트 | `#075332` | `#0A4630` |
| 나무 테두리 | `#6B3E20` | `#7A4A22` |

⚠️ **파란 회색을 다시 넣지 마세요.** 한 색이라도 섞이면 그 화면만 사무적으로 보입니다.
⚠️ **금색은 한 색으로 쓰지 마세요.** 한 면에 청동과 샴페인이 같이 있어야 금속으로 보입니다.

**화면 바탕이 스물세 가지였습니다.** 게임마다 파랑·초록·회색으로 제각각이라 화면을 옮길
때마다 다른 앱 같았습니다. 이제 **바탕은 `colors.bg` 하나**이고 판(펠트·바다·트랙)만 색을
가집니다. 첫 화면(`splash`·`door`)만 사진이 있어 예외입니다.

**글씨체를 처음 넣었습니다.** 그전에는 앱 전체에 `fontFamily`가 **한 줄도 없었습니다** —
"보고서 느낌"의 절반이 여기서 왔습니다.

- `public/index.html`에서 구글 폰트를 받습니다 — 로마자 제목 `Cinzel`, 나머지 `Noto Sans KR`
- ⚠️ **`body`에 걸면 안 먹습니다.** react-native-web이 글자마다 기본 글꼴을 직접 박아
  넣기 때문입니다. `html body #root *`처럼 **#root를 낀 선택자**라야 이깁니다
- 제목 세리프는 `displayFont`를 펼쳐 넣은 곳만 적용됩니다(`dataSet` → `data-display`).
  **한글이 없는 글꼴이라 로마자에만 쓰세요**

**빛** — `roomLight`(위 가운데 금빛)와 `roomVignette`(구석 어둡게). 둘 다 **그림자로만**
그려서 높이를 한 칸도 안 먹습니다. 화려함을 자리 안 먹는 것부터 넣은 것입니다.

**금속 버튼** — `primaryButton`의 위·왼쪽 테두리는 샴페인, 오른쪽·아래는 청동. 두 줄만으로
납작한 노랑이 금처럼 보입니다. 패널들은 위쪽에만 `rgba(245,222,138,0.22)` 실선을 둡니다.

### 카드가 판에 가려지던 것

⚠️ **`dealerFelt`의 아래 모서리를 190으로 두면 안 됩니다.** 반원처럼 보이라고 준 값인데
`overflow: hidden`과 맞물려 **맨 아랫줄 바깥쪽 카드의 모서리를 잘라먹었습니다.**
파이 고우는 카드가 6px, 바카라는 베팅 자리가 8px 잘렸습니다. **60으로 낮췄습니다.**

**`useCardFit`** — 자리를 재서 카드 크기 단계를 고릅니다(`useTableDeal` 옆).

- 단계 넷: `big 72×108` · `mid 58×88` · `small 46×70` · `mini 40×62`(새로 넣음)
- 높이와 **너비를 같이** 봅니다. ⚠️ 너비 계산에 **줄의 `gap`을 꼭 넣으세요** —
  빼먹으면 계산상 들어가는데 실제로는 줄이 접힙니다(파이 고우에서 한 번 겪었습니다)
- `crowded`가 false면 **안 겹치고 나란히** 놓습니다. 두 장짜리를 굳이 겹칠 이유가 없습니다
- `spare`는 **재서 넣는 값**입니다. 파이 고우 394, 블랙잭 350(스플릿이면 369)

**겹치는 정도를 0.48 → 0.68로 벌렸습니다**(`cardFanSpread`). 절반 넘게 가려져 무슨 카드인지
읽기 어려웠습니다. 더 벌리면 줄이 넓어지지만 `useCardFit`이 크기를 한 단계 내려 맞춥니다.

**블랙잭**

- 처음 넉 장이 **나 · 딜러 · 나 · 딜러** 한 장씩 놓입니다(`useTableDeal`)
- 히트로 손이 커지면 카드가 작아집니다. 전에는 **다섯 장부터 줄이 접혀 판 아래로 잘려**
  딜러가 뽑은 카드가 안 보였습니다
- 순서는 원래 블랙잭 그대로입니다 — 내가 다 하고, 죽으면 거기서 끝, 안 죽으면 그때
  딜러가 17까지 뽑습니다. 딜러 뒷장은 `DEALER_REVEAL_MS` 0.62초마다 한 장씩 열립니다

**조커 포커 설명을 5줄 → 28줄**로 늘렸습니다. 점수 셈, 계산 예시, 족보 아홉 개의 기본값,
조커 여덟 종이 하는 일, 왜 조커가 노릴 족보를 정하는지, 요령, 배당까지.

## 2026-08-31 (저녁) — 카드가 한 장씩 깔립니다 · 비디오 포커 연출

**공용 도구 하나를 만들었습니다** — `useTableDeal`. `useReveal`/`RevealButton` 바로 옆에
있습니다. 게임마다 따로 고치면 32번 고쳐야 해서 하나로 뒀습니다.

- `countFor(seat)` — 그 자리에 지금 몇 장이 놓였는지. 화면은 이 수만큼만 그립니다
- `dealing` — 아직 나눠 주는 중인지. **이때는 컴퓨터도 나도 못 둡니다**
- 첫 번째 인자에는 **판마다 새로 만들어지는 것**(대개 `hands` 배열)을 넘깁니다.
  장수만 보면 같은 장수로 새 판을 시작할 때(홀덤은 늘 두 장) 다시 안 깔립니다.
  빅투는 `state`가 한 장 낼 때마다 바뀌어서 판 번호(`dealKey`)를 따로 셉니다

**속도는 도구가 정합니다.**

| 값 | 뜻 |
| --- | --- |
| `DEAL_MINE_MS` 200 | 내 카드(0번 자리). 눈으로 읽어야 하니 천천히 |
| `DEAL_THEIRS_MS` 100 | 남의 카드. 뒷면이라 읽을 것이 없습니다 |
| `DEAL_BUDGET_MS` 1600 | **다 깔리기까지의 목표 시간.** 넘치면 그만큼 간격을 줄입니다 |

자리 수만큼 돌아가며 한 장씩 놓아 실제로 딜러가 나눠 주는 것처럼 보입니다.
세븐 포커 첫 거리(네 명 12장)는 1.5초라 줄일 것이 없어 그대로 가고, 차이니즈 포커
(네 명 52장)는 그대로면 6.5초라 간격을 4분의 1로 줄입니다.

**넣은 곳 — 여덟 화면**: 홀덤 · 오마하 · 세븐 포커 · 하이로우 · 틴 파티 · 파이 고우 ·
차이니즈 포커 · 빅투.

**안 넣은 곳과 이유**

- **블랙잭 · 바카라 · 비디오 포커 · 파이 고우 딜러 패**는 이미 한 장씩 엽니다.
  블랙잭은 `DEALER_REVEAL_MS` 0.62초로 저절로, 바카라는 누를 때마다 한 장,
  파이 고우와 틴 파티의 상대 패는 `RevealButton`입니다. 여기 손대면 그 연출이 겹칩니다
- **화투(고스톱·맞고·섰다·도리짓고땡·투전)와 마작**은 카드 판이 아니라 손패를 다루는
  화면이라 자리별 장수라는 개념이 안 맞습니다. 필요하면 따로 봐야 합니다

**비디오 포커를 화려하게** 했습니다.

- 배당표를 아홉 줄로 쪼개(`videoPokerPaytable`) **맞은 줄에만 불이 들어옵니다**
- **족보를 이루는 카드만** 금테를 두르고 번쩍입니다(0.25초씩 다섯 번).
  어느 카드가 아닌지는 `src/videopoker.ts`의 `videoPokerMadeCards`가 정합니다 —
  다섯 장을 다 쓰는 족보는 다섯 장, 짝으로 되는 족보는 짝이 된 카드만
- **이길 때 코인 아홉 개가 튑니다**(`videoPokerCoinLayer`). 판 위에 겹쳐 놓아
  자리를 안 차지합니다
- 테스트 하나를 새로 넣었습니다(`videoPokerMadeCards`). **653개 → 654개**

⚠️ 아이폰 375×812에서 여덟 화면 모두 **스크롤이 안 생기는 것**을 다시 쟀습니다(높이 812).

## 2026-08-31 (낮) — 물고기 룰렛: 원 하나로 · 문어 넣기

**원이 하나가 됐습니다.** 남색 판(`fishRouletteBoard`)에 칠하던 색과 테두리를 걷어 내
**자리 잡는 틀로만** 남기고, 보이는 원은 물 하나(지름 248)로 했습니다. 자리 12곳은 그
**바깥** 반지름 140에 붙였습니다.

| 이름 | 값 | 왜 이 값인가 |
| --- | --- | --- |
| `fishRouletteWaterSize` | 248 (반지름 124) | 하나뿐인 보이는 원 |
| `fishRouletteRing` | 140 | 자리 12곳. 지름 36이라 안쪽 끝 122가 물 테두리 124에 2만큼 물립니다. 바깥 끝은 158이라 틀 160 안에 들어옵니다 |
| `fishRouletteFishRing` | 118 | 들어간 물고기가 서는 곳. 바깥 끝 129라 물을 조금 넘어 자리 밑에 파고듭니다 |
| 헤엄치는 띠 | 59~100 | 위 값의 0.50~0.85(`src/fishroulette.ts`의 `startRadius`). 물 안에서만 돕니다 |

**이 넷은 서로 맞물려 있습니다.** 하나를 바꾸면 나머지도 같이 보세요.
아이폰 375×812에서 **스크롤이 안 생기는 것**을 다시 쟀습니다(문서 높이 812).

**문어를 넣었습니다.** 승식이 여섯에서 **일곱**이 됐습니다.

- 문어는 **느립니다.** 8~21초 사이에 앉습니다(보통 물고기는 3~20.9초)
- 앉은 자리와 **시계 방향 옆 칸** 두 칸을 차지합니다(5번이면 5·6번). 시계 방향 고정입니다
- 그 두 칸에는 **물고기가 아예 못 들어갑니다.** 판이 시작될 때부터 막혀 있습니다
- **기존 여섯 승식에서는 물고기로 안 셉니다.** 그래서 막힌 두 칸은 '없음'이 반드시 이기고
  '먼저'·'많이'는 반드시 집니다
- **20초 안에 못 앉으면 안 셉니다**(7.68%). 그때 '문어 자리'는 어디에 걸었든 꽝입니다.
  막는 것은 그대로입니다 — 문어가 두 칸 위를 맴돌아 물고기가 못 들어온 것으로 봅니다
- 화면: 막힌 두 칸은 **보라색**(`fishRouletteSlotBlocked`)에 `문어`라고 적고, 🐙는 두 칸
  한가운데에 앉습니다(`octopusAngle` = 자리 각도 + 15°). **문어가 앉은 뒤에만** 칠합니다.
  미리 칠하면 '문어 자리'의 답이 새 나갑니다

**배당을 200만 판 돌려 다시 쟀습니다.** 실제로 바뀐 것은 '없음' 하나뿐입니다.

| 승식 | 잰 확률 | 정한 배당 | 잰 환급률 | 문어 넣기 전 |
| --- | --- | --- | --- | --- |
| 한 자리 · 먼저 | 8.34% | 11.40배 | 95.0% | 그대로 |
| 한 자리 · 많이 | 8.33% | 11.40배 | 95.0% | 그대로 |
| 한 자리 · 없음 | 41.80% | **2.27배** | 94.9% | 37.16% · 2.55배 |
| 이웃 두 자리 · 먼저 | 16.64% | 5.70배 | 95.0% | 그대로 |
| **문어 자리** | 7.68% | **12.37배** | 95.0% | 새로 넣음 |
| 홀 / 짝 · 먼저 | 50.05% | 1.90배 | 95.1% | 그대로 |
| 앞 절반 / 뒤 절반 | 49.98% | 1.90배 | 95.0% | 그대로 |

- '없음'이 오른 이유: 막힌 두 칸(2/12 = 16.67%)은 반드시 이기고, 남은 열 칸에는 물고기가
  더 빽빽하게 몰립니다. 16.67% + (10/12)×(9/10)^11.4 ≈ 41.8%로 계산과 맞습니다
- **자리 12곳은 여전히 고릅니다.** 문어가 막는 두 칸이 붙어서 돌기만 하므로 대칭이 안 깨집니다.
  '먼저' 8.30~8.38% · '많이' 8.30~8.36% · '문어' 7.66~7.73%
- 잰 값은 `src/fishroulette.ts` 맨 아래 주석에 그대로 남겨 두었습니다
- 테스트를 일곱 개 새로 넣었습니다(막은 두 칸에 물고기가 없는지 · 못 앉았을 때 꽝인지 ·
  요약 줄 · 문어가 두 칸 한가운데에 서는지 등). **646개 → 653개**

## 2026-08-30 (밤) — 카드 화면 · 분류 이름 · 룰렛 · 고스톱 버그

**분류** — `룰렛`을 자동 배팅에서 주사위로 옮기고 `개인 카드` → **카드**,
`뱅커 카드` → **딜러**로 이름을 바꿨습니다. 지갑의 카테고리별 손익은 `gameCategoryOf`가
게임 이름으로 그때그때 찾으므로 **예전 기록도 새 이름으로 묶여 나옵니다.**

**카드 화면 네 곳** (세븐 포커 · 하이로우 · 홀덤 · 오마하)

- 겹쳐 쌓는 카드는 **숫자와 무늬를 왼쪽 위에 나란히** 그립니다(`stacked`, 진짜 카드의
  모서리 표시). 22만 보여도 둘 다 읽힙니다.
  ⚠️ **한 번 눕혀 봤다가 글자가 누워 읽기 나빠서 무르고 다시 세웠습니다**
- 상대 카드 40×62 → **46×70** · 위 자리는 12만 겹쳐 일곱 장이 한 줄에 다 펴집니다
- 내 공개 카드만 12 들어 올림 · 폴드·체크·레이즈를 **판 가운데 앞쪽**으로 옮김
- 자리마다 **행동 표시**(폴드·콜·체크·레이즈, 1.6초) · **낸 돈을 칩 그림**으로
- **상대방 돈**을 보여 줍니다. 컴퓨터마다 베팅 한 판 값의 40배를 들고 앉고 판마다 딴 만큼
  늘고 잃은 만큼 줍니다(`useSeatStacks`). 한 판 값도 안 남으면 새로 채워 앉습니다.
  ⚠️ **보여 주기용입니다** — `src/table.ts`는 여전히 컴퓨터를 칩 무한으로 다뤄
  올인·사이드 팟이 없습니다. 진짜로 떨어지게 하려면 그 파일부터 고쳐야 합니다
- **마지막 승부는 한 번에 공개**하고 누가 무엇이 됐는지 한 줄로 보여 줍니다(`handSummary`).
  폴드로 덮인 패는 그 줄에서 빼서 **상대 패가 새지 않게** 했습니다
- 판이 늘었다 줄었다 하던 것을 전부 고정했습니다(위 "고정해 둔 값")

**블랙잭** — 딜러가 버튼 없이 스스로 뒷장과 뽑는 카드를 0.62초마다 엽니다.
**비디오 포커** — DRAW를 누르면 바꾸는 카드만 좌우로 네 번 돌다가 새 패를 보여 줍니다.
**고스톱** — 치는 순간 **가져오는 쪽은 크게(52×78), 못 가져오는 쪽은 작게(40×60)** 그리고
못 가져온 쪽은 테두리에만 옅은 빛을 남깁니다. 글로도 적습니다(`못 가져옴` · `그냥 깜`).

**룰렛** — 두 단계로 나눠 돌리던 것을 **감속 곡선 하나**로 합쳤습니다(`Easing.out(cubic)` ·
4.8초). 1단계가 끝에서 105°/초까지 느려진 뒤 2단계가 318°/초로 시작해서 "중간에 한 번 더
세게 도는" 것처럼 보였습니다. 한 곡선이라 이제 속도가 튈 자리가 없습니다.

**고친 버그 — 고스톱이 폭탄에서 멈추던 것 (판정 로직 두 가지)**

시뮬레이션 4,000판 중 **8.3%가 멈췄고 고친 뒤 0%**입니다. 테스트 세 개를 새로 넣었습니다
(폭탄 뒤 두 장 짝 · 빈손 건너뛰기 · 300판 끝까지 돌리기).

1. **폭탄 뒤 뒤집은 패가 바닥 두 장과 맞으면 예외가 났습니다.** `playGoStopBomb`이 고를
   패를 안 넘겨서 "같은 월 두 장 중 가져갈 패를 골라야 합니다"로 터졌습니다. 컴퓨터 차례는
   예약된 작업 안에서 도는데 거기서 터지면 그 차례가 안 끝나 판이 통째로 멈춥니다
2. **폭탄으로 손이 먼저 빈 사람에게 차례가 갔습니다.** 폭탄은 세 장을 한 번에 내니 그
   사람만 두 차례 먼저 비는데, 빈손에 차례가 가면 낼 패가 없어 그대로 멈춥니다.
   → `nextPlayerWithCards`로 **패가 남은 다음 사람**에게 넘깁니다

⚠️ 둘의 공통 구조: **판이 하나도 안 바뀌면 다음 차례를 부를 계기가 사라집니다.** 한 번
멈추면 영영 멈춥니다. 컴퓨터 차례를 예약으로 도는 화면을 고칠 때는 이 점을 꼭 보세요.

## 2026-08-30 (낮) — 인원 2~4명 · 파치슬롯 · 예측 마켓 둘로

**카드 여섯 게임에 인원(나 포함) 2·3·4명**을 넣었습니다. 베팅 라운드는 새로 만든
`src/table.ts` 하나를 네 게임이 같이 씁니다.

- 자리 0번이 나, 1번부터가 컴퓨터. 라운드는 **살아 있는 사람이 모두 한 번씩 행동했고 낸
  돈이 같아졌을 때** 끝납니다
- **사이드 팟은 안 만듭니다.** 컴퓨터는 칩이 무한해 올인이 없습니다
- 컴퓨터 승률은 **1대1 승률을 상대 수만큼 곱해서** 씁니다(`multiwayEquity`).
  이걸 안 하면 네 명 판에서 1대1처럼 밀어붙입니다
- 자리는 위·좌·우로 나눠 앉힙니다(`tableSeatSpots`). 나는 늘 아래입니다
- 차이니즈 포커는 **상대마다 따로 겨뤄 값을 더합니다**(`resolveChinesePokerTable`)
- 고친 버그: 시작 버튼이 컴퓨터 차례에도 눌려 판이 새로 시작되던 것(`busy`로 잠금)

**파치슬롯**을 실제 기기 구조로 다시 만들었습니다(`src/pachislot.ts`). 목록에는 여전히
`슬롯` 하나이고 그 안에서 클래식/파치슬롯을 고릅니다.

- **천장** 999게임 · **찬스존** 150게임마다 5게임(성공률 22.6%) · AT 순증 2.42매/G
- **200만 게임을 돌려 재서** 환급률 95.03%에 맞췄습니다. 맞추는 손잡이는 `pachiAtContinue`
- 옛 로직(`src/slot.ts`의 `evaluatePachislot`)은 지우지 않고 그대로 뒀습니다

**예측 마켓**을 스포츠(球)와 사회문제(社) 둘로 나눴습니다. 화면은 `PredictGameScreen`
하나를 `group`으로 갈라 씁니다.

**바카라 뱅커 자리는 안 바꿉니다** — 블랙잭의 딜러는 내 상대지만 **바카라의 뱅커는 내가 걸
수 있는 두 쪽 중 하나**입니다. 실제 카지노도 두 패를 테이블 가운데에 나란히 깝니다.

## 2026-08-29~30 — 한 화면 고정 · 화투패 · 경륜

- **카드 게임 열세 화면을 한 화면에 고정**했습니다(아이폰 375×812에서 스크롤 없음)
- **반원형 딜러 테이블** — 블랙잭 · 바카라 · 파이 고우 · 틴 파티
- **고스톱 판을 실제 판처럼** — 위가 상대, 가운데가 바닥과 더미, 아래가 내 자리와 손패
- **화투패 48장을 새 그림으로.** 자르지 않고 통째로 줄여 넣어 48장이 전부 **150×225**로
  같습니다. 예전에는 여백을 잘라내서 제각각이었고 그게 지저분해 보이던 원인이었습니다
  - ⚠️ **`scripts/trim-hwatu.mjs`를 다시 돌리지 마세요.** 여백을 또 잘라 크기가 제각각이 됩니다
  - ⚠️ 새 그림을 받으면 **방향도 꼭 확인하세요.** 11월 피 두 장이 거꾸로 들어와 180도 돌렸습니다
  - 월별 자리 순서는 `src/hwatu.ts`의 `monthSpecs` 그대로(0번이 광 또는 열끗, 1번이 띠, 2·3번이 피)
- **경륜**을 세로로 긴 타원(344×424)으로. 작게 만든 것은 트랙이 아니라 **선수**입니다(30 → 20)
- **공 어디에?는 접었습니다** — 컵마다 색이 달라 색만 따라가면 100% 맞았고 환급률이 300%였습니다
- **막대 정리** — 위 68 → 40, 아래 탭바 72 → 52. 게임 중에는 위 막대가 아예 안 나옵니다
- **오래 헤맨 것 — 시계 아래 가짜 빈 띠**: `public/index.html`이 상태바를 `black`으로 두어
  iOS가 이미 시계 아래에서 화면을 시작해 줍니다. 그래서 `env(safe-area-inset-top)`이 0으로
  나오는 게 정상인데 코드가 "못 읽었다"로 보고 44를 강제로 넣고 있었습니다. 그 44가
  통째로 빈 띠였습니다. 시계·배터리는 iOS가 그리는 것이라 **웹 앱이 끌 수 없습니다**
