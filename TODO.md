# 다음에 할 일 (2026-08-30 갱신)

저장소: https://github.com/babodabin/casino · 배포: https://babodabin.github.io/casino
main에 푸시하면 GitHub Actions가 자동 배포합니다. **푸시 = 라이브 배포**입니다.

**이 문서 하나만 읽으면 바로 시작할 수 있게 적었습니다.** 코드를 뒤져 상황을 파악할
필요가 없도록, 어디를 고치는지와 왜 그렇게 되어 있는지를 같이 적어 두었습니다.
**바로 시작하려면** 아래 **0-7(물고기 룰렛 다시)** 부터 보시면 됩니다.
그다음이 0-3(카드 바뀌는 속도)입니다. **0-3은 0-7이 끝난 뒤에 시작하기로 정했습니다.**
0-1(분류) · 0-2(인원 2~4명) · 0-4(파치슬롯) · 0-5(바카라) · 0-6(개인 카드 화면) · 1번은 끝났습니다.
**0번들이 서로 어떻게 얽히는지는 "0-0. 겹치는 것"에 적어 두었습니다. 그것부터 읽으세요.**


---

## 바로 시작할 것

8월 30일에 받은 지시입니다. 위에서부터가 급한 순서지만 순서는 바꿔도 됩니다.

## 0. 8월 30일 늦게 받은 지시

### 0-0. 겹치는 것 — 손대기 전에 이것부터

미리 훑어보고 적어 둔 것입니다. 순서를 틀리면 두 번 일하게 됩니다.

1. **0-1(분류)과 0-2(네모 테이블)는 같은 선입니다.** 개인/뱅커로 가른 선이 곧 테이블
   모양입니다 — **뱅커 카드 6개는 반원형(`DealerTable`) 유지, 개인 카드 6개는 네모.**
   아래 4번의 "1대 다수 순서(바카라→블랙잭→파이 고우→홀덤)"가 이 둘로 갈라집니다
2. ~~0-2 앞에 3번(카드 크기 자동)이 먼저입니다.~~ 0-2를 먼저 했습니다. 상대 카드를
   `tiny`(40×62)로 줄여서 네 명까지 들어갔습니다. **3번을 할 때 이 크기를 단계 중 하나로 넣으세요**
3. **0-3(속도)과 0-2(1대 다수)가 곱해집니다.** 한 장에 0.2초인데 사람이 4명이면 기다림이
   4배입니다. 도구를 만들 때 **"내 차례는 천천히, 남의 차례는 빠르게"** 를 같이 정해야 합니다.
   고스톱이 한 사람당 2초인데 그게 기준선입니다
4. **속도 도구는 `useReveal`/`RevealButton` 옆에 둡니다.** 따로 만들면 게임마다 속도가 달라집니다
5. **빈 칸은 주사위 2 · 마작 2뿐입니다**(나머지 넷은 6개로 꽉 참). 되살릴 게임을 넣을 때
   알아 둘 것이지, 지금 고칠 것은 아닙니다 — **8월 30일에 그냥 두기로 정했습니다.**
   - 주사위 두 칸에는 들어갈 것이 있습니다: 행운의 물고기 · 코인 푸셔 · 공 어디에? · 홀짝,
     그리고 1-3의 후보인 인형뽑기 · 두더지 잡기 · 다트
   - 마작 두 칸은 마작이 아니면 못 들어갑니다
   - **막히는 것은 둘뿐**입니다: 새 카드 게임(파이브 카드 드로우 등)과
     새 배팅 게임(로또 · 즉석 복권 · 경정 · 자동차 레이스 · 소싸움). 그때 가서 정하면 됩니다
     (6개 규칙을 7개로 늘리거나 · 분류를 하나 더 만들거나 · 있는 것 하나를 빼거나)

### 0-1. ~~분류 여섯 개로 다시 나누기~~ — 끝났습니다 (8월 30일)

카드 게임을 **개인/뱅커로 가르고**, 슬롯·룰렛·경주·예측처럼 걸어 놓고 결과만 기다리는 것을
**자동 배팅**으로 모았습니다. 윷 베팅은 한국 전통에 그대로 두었습니다.

| 분류 | 게임 | 개수 |
| --- | --- | --- |
| 개인 카드 | 홀덤 · 오마하 · 세븐 포커 · 하이로우 · 차이니즈 포커 · 빅투 | 6 (꽉) |
| 주사위 | 야찌 · 크랩스 · 식보 · 물고기 룰렛 | 4 |
| 뱅커 카드 | 블랙잭 · 바카라 · 파이 고우 · 틴 파티 · 비디오 포커 · 조커 포커 | 6 (꽉) |
| 자동 배팅 | 슬롯 · 룰렛 · 경마 · 경륜 · 예측 마켓(스포츠) · 예측 마켓(사회문제) | 6 (꽉) |
| 한국 전통 | 고스톱 · 맞고 · 섰다 · 도리짓고땡 · 투전 · 윷 베팅 | 6 (꽉) |
| 마작 | 리치 · 중국식 · 홍콩 · 사천 | 4 |

- 게임 32개는 그대로입니다. 하나도 안 빠졌는지 스크립트로 대조했습니다
- 야찌 아이콘이 크랩스와 겹쳐서 ⚄ → ⚅로 바꿨습니다
- **지갑의 카테고리별 손익은 게임 이름으로 그때그때 계산합니다**(`gameCategoryOf`).
  그래서 예전 기록도 새 분류로 다시 묶여 나옵니다. 기록이 사라지지는 않습니다

### 0-2. ~~개인 카드를 1~4명으로~~ — 끝났습니다 (8월 30일)

개인 카드 여섯 게임 전부 준비 화면에서 **인원(나 포함) 2·3·4명**을 고를 수 있습니다.
`PlayerCountRow`가 그 줄이고, 고른 값은 `tablePlayers` 하나를 여섯 게임이 같이 씁니다.

| 게임 | 어떻게 했나 |
| --- | --- |
| 빅투 | 로직이 이미 2~4명을 받고 있어서 화면만 이었습니다. 배당도 인원별로 이미 있었습니다(1.6 · 2.3 · 2.95배) |
| 차이니즈 포커 | `resolveChinesePokerTable` — **상대마다 따로 겨뤄 값을 더합니다.** 상대 수로 나눠 눈금을 맞춰서 몇 명이든 "모두에게 세 줄을 다 이기면 2배"입니다 |
| 세븐 포커 · 하이로우 · 홀덤 · 오마하 | 베팅 라운드를 새로 만든 `src/table.ts`로 갈아 끼웠습니다 |

**`src/table.ts`가 핵심입니다.** 네 게임이 이 하나를 같이 씁니다.

- 자리 0번이 나, 1번부터가 컴퓨터입니다. 차례는 번호 순으로 돕니다
- 라운드는 **살아 있는 사람이 모두 한 번씩 행동했고 낸 돈이 같아졌을 때** 끝납니다.
  누가 올리면 그 뒤로 다시 한 바퀴 돌아야 합니다
- **사이드 팟은 안 만듭니다.** 컴퓨터는 칩이 무한해 올인이 없고 받거나 폴드만 합니다.
  폴드한 사람 돈은 팟에 남고 끝까지 간 사람들은 낸 돈이 언제나 같습니다
- 컴퓨터 승률은 **1대1 승률을 상대 수만큼 곱해서** 씁니다(`multiwayEquity`).
  이걸 안 하면 네 명 판에서 1대1처럼 밀어붙입니다
- 테스트 14개. 그중 하나는 **네 명 판을 200번 돌려** 막히지 않고 팟이 맞는지 봅니다

화면 쪽:

- **자리를 위·좌·우로 나눠 앉힙니다**(`tableSeatSpots`). 둘이면 마주 보고, 셋이면 좌우,
  넷이면 좌·위·우입니다. 나는 늘 아래입니다
- 좌우 자리는 폭이 62뿐이라 **카드를 세로로 겹쳐 쌓고**(`tableSeatCardsColumn`)
  이름줄도 세로로 놓습니다(`tableSeatHeadColumn`). 가로로 두면 "컴퓨터 폴/1 드"처럼 쪼개집니다
- 상대는 `tiny` 카드(40×62), 나만 큰 카드입니다. 네 명이 7장씩 들면 안 그러면 안 들어갑니다
- 아이폰(375×812)에서 네 명 승부까지 스크롤 없이 들어가는 것을 실제로 눌러 확인했습니다
- 폴드한 사람은 흐려지고 승부 때 카드를 안 엽니다

### 0-2-1. 고친 버그 — 판이 저절로 새로 시작되던 것 (8월 30일)

**증상**: 텍사스 홀덤에서 결과가 안 나오고 카드만 돌았습니다. 중간에 카드가 바뀌고
공용 카드가 사라지기도 했습니다.

**원인**: 시작 버튼이 컴퓨터 차례에도 눌렸습니다. "진행 중"이라고 적혀만 있고 실제로는
`start()`가 걸려 있어서, 누르면 판이 통째로 새로 시작됐습니다. 세븐 포커·하이로우도
같은 코드라 똑같았습니다.

**고친 것**: `busy`(판이 도는 중)를 만들어 그동안 버튼을 잠갔습니다.
세 화면 모두 `disabled={busy||...}`이고 `onPress`도 막았습니다.

⚠️ 인원을 늘리면 **한 판이 길어집니다.** 컴퓨터 한 명당 0.7초씩 쉽니다(`TABLE_THINK_MS`).
네 명이면 한 라운드에 2초가 더 듭니다. 0-3(속도)을 할 때 같이 보세요.

### 0-6. ~~개인 카드 화면 다듬기~~ — 끝났습니다 (8월 30일)

4명 판을 직접 해 보고 주신 여섯 가지를 다 고쳤습니다. **로직은 안 건드렸습니다.**
`SevenPokerGameScreen` · `HighLowGameScreen` · `PokerGameScreen` 세 곳을 같이 고쳤고,
새로 만든 것은 셋이 같이 씁니다.

| 지시 | 어떻게 했나 |
| --- | --- |
| 1. 좌우 자리 카드 눕히기 | `PlayingCard`에 `sideways`를 넣었습니다 |
| 2. 내 공개 카드 들어 올리기 | 공개 카드만 12만큼 올립니다(`tableMyOpenCard`) |
| 3. 버튼을 판 가운데 앞쪽으로 | 팟과 내 자리 사이에 놓았습니다(`tableFrontRow`) |
| 4. 내 카드 고정 | 자리 높이·폭을 미리 잡아 뒀습니다 |
| 5. 컴퓨터 행동을 그 자리에 | `useSeatActions` · `tableSeatActionWrap` |
| 6. 자리 앞에 칩 | `SeatChips` |

**1. 눕힌 카드(`sideways`)** — 카드를 통째로 왼쪽 90도로 돌립니다. 글자도 같이 눕지만
무늬를 21로 크게 그려 멀리서도 ♦인지 ♥인지 보입니다.

- 요령은 **글자를 카드의 오른쪽 끝에 붙인 것**입니다(`alignItems:'flex-end'`).
  왼쪽으로 눕히면 오른쪽 끝이 위쪽 끝으로 갑니다. 그래서 아래를 겹쳐 쌓아도
  숫자와 무늬가 그대로 보입니다. 22만 남기고 겹칩니다(`tableSideCardStack`)
- 눕힌 카드가 들어가는 칸은 가로세로를 바꾼 66×44입니다(`tableSideCardBox`).
  안쪽 2가 남아 비공개(금색)·공개(초록) 테두리가 그대로 보입니다
- ⚠️ **강조 스타일(`cardWinner` 등)이 `transform`을 통째로 바꿉니다.** 그러면 눕힌 카드가
  다시 일어섭니다. 그래서 회전을 넣은 같은 값을 뒤에 한 번 더 얹었습니다(`sidewaysWinner` 등).
  **transform을 건드리는 스타일을 새로 만들 때는 이걸 꼭 같이 보세요**
- 좌우 자리 폭을 62 → 66으로 넓혔습니다. 그만큼 가운데가 좁아져서(295 − 66×2 = 163)
  **홀덤 공용 카드 다섯 장이 좌우 자리를 덮었습니다.** 겹침을 −30에서 −34로 좁히고
  사이 간격을 없애 154로 줄였습니다(`pokerBoardRow` · `pokerBoardFan`).
  이건 원래도 좌우를 덮고 있던 것을 이번에 같이 고친 것입니다

**4. 안 움직이게 한 방법** — 자리가 늘었다 줄었다 하는 곳을 전부 미리 잡아 뒀습니다.

- 좌우 자리 높이 210 고정(`tableSideSlotTall`) — 일곱 장이 다 와도 안 늘어납니다
- 내 카드 줄 폭 302 고정(`tableMyCardsSeven`) — 가운데 정렬이면 장수가 늘 때마다 왼쪽으로 밀립니다
- 판 가운데 앞줄 56 · 아래 버튼 자리 52 · 결과 줄 24 고정
- 칩 더미 높이 29 고정 — 칩이 한 개에서 세 개가 돼도 자리가 안 커집니다
- **실제로 재 봤습니다**: 세븐 포커 4명 판에서 첫 3장부터 여섯 번째 카드까지
  내 자리 위치가 512로 그대로입니다. 승부 때만 5 움직입니다(하이로우는 8)

**나머지** — 내 차례에는 아래 버튼 자리에 "판 가운데 버튼으로 고르세요"가 들어갑니다.
자리를 비워 두면 판이 위아래로 흔들려서, 빈 채로 두지 않고 글을 넣었습니다.
자리 표시(`note`)는 없애지 않고 판 가운데 앞줄로 옮겼습니다. 금액은 거기서 읽고,
자리에 뜨는 표시는 폭이 66뿐이라 `폴드`·`콜`·`체크`·`레이즈`만 씁니다(1.6초 뒤 사라짐).

⚠️ **칩은 이름줄에 놓았습니다.** "자리 앞"이라면 위 자리는 카드 아래가 맞지만,
그러면 세로로 29가 더 들어 자리가 모자랍니다. 내 자리는 카드 바로 위(=팟 쪽)라 앞이 맞습니다.
**한 화면에 담는 것을 먼저 지켰습니다.** 자리를 옮기려면 3번(카드 크기 자동)과 같이 해야 합니다.

⚠️ **아직 안 고친 것 — 위 자리 카드가 서로 너무 겹칩니다.** 일곱 장을 `-22`로 겹쳐서
무늬가 아래쪽만 잘려 보입니다(`sevenPokerCardOverlap`). 이번 여섯 가지에 없어서 그대로 뒀습니다.
3번(카드 크기 자동)을 할 때 같이 보세요.

### 0-7. 물고기 룰렛 — 다시 ← **여기부터** (제가 잘못 알아들었습니다)

**"큰 원이 있고 1~12는 큰 원 밖에"** 라는 뜻이었는데, 저는 원 가장자리 안쪽으로
밀어 넣었습니다. 자리 12곳을 **원 바깥에** 두어야 합니다.

- 지금: 판 320 · 물 232 · 자리가 반지름 134(원 테두리 안쪽에 박혀 있음)
- 할 것: **물(큰 원)을 하나 두고 자리 12곳은 그 바깥에** 놓기.
  물 지름을 자리 안쪽 지름에 맞추고, 자리는 물 바깥 테두리에 걸치게 합니다
- 물고기가 자리로 들어가면 어디에 서는지도 같이 정해야 합니다(지금은 자리 안쪽 106)

**대형 물고기(또는 문어) 한 마리 추가**

- 보통 물고기 12마리 말고 **큰 놈 한 마리**를 더 넣고, **그것도 베팅 대상**으로 만듭니다
- **정한 것(8월 30일): 문어는 느리고 자리를 두 칸 차지합니다.**
  - 보통 물고기보다 늦게 들어가고, 들어가면 그 자리와 옆 자리까지 **두 칸을 막습니다**
  - 그래서 "문어가 어디 들어가나"에 걸 수 있고, 문어가 막은 칸은 다른 승식에도 영향을 줍니다
- ⚠️ 아직 정해야 할 것:
  - 문어가 막은 자리에 다른 물고기가 들어갈 수 있나, 아니면 못 들어가나
  - 두 칸이 **어느 쪽 옆 칸**인가 — 시계 방향으로 고정할지, 들어간 방향에 따라 달라질지
  - 승식 이름과 뜻 — "문어 자리" / "문어가 먼저" 같은 것
  - **배당은 짐작하지 말고 시뮬레이션으로 재서** 정합니다(`src/fishroulette.ts` 맨 아래
    주석에 200만 판 잰 값이 있으니 같은 방식으로 다시 잽니다)
  - ⚠️ 문어를 넣으면 **기존 여섯 승식의 확률이 다 바뀝니다.** 자리를 두 칸 막으니
    "없음"이 크게 늘어납니다. **여섯 개 배당을 전부 다시 재야 합니다**

### 0-3. 카드 바뀌는 속도 — 전 게임 공통 (0-7 뒤에)

**지금은 상태를 한 번에 바꿔서 눈에 안 보이게 순식간에 바뀝니다.** 게임마다 따로 고치면
32번 고쳐야 하니 **공용 도구 하나**를 만들어 씁니다.

- 한 장씩 0.2초 간격으로 뒤집기. `useReveal`/`RevealButton` 옆에 둡니다
- **"내 차례는 천천히, 남의 차례는 빠르게"** 를 도구가 정하게 합니다(0-0의 3번)
- 비디오 포커는 여기에 더해 **화려하게**: ① 카드가 뒤집히며 도는 움직임 ②
  족보가 맞으면 그 카드만 번쩍이고 배당표에서 맞은 줄이 켜짐 ③ 이길 때 코인이 튀는 효과.
  **셋 다 하기로 했습니다**

### 0-4. ~~파치슬롯을 실제 구조로~~ — 끝났습니다 (8월 30일)

붙여 주신 파칭코/슬롯 비교 글대로 고쳤습니다. 로직은 `src/pachislot.ts`(새 파일),
화면은 `PachislotGameScreen`입니다. **목록에는 여전히 `슬롯` 하나**이고 그 안에서
클래식/파치슬롯을 고릅니다. 그래서 분류 자리를 안 씁니다.

- **천장** — 999게임을 돌리면 무조건 당첨. 천장으로 들어가면 은혜로 AT가 두 배(100게임)
- **찬스존** — 150게임마다 5게임짜리 기회 구간. 다섯 번 중 한 번꼴(22.6%)로 성공
- **순증** — AT에 들어가면 1게임마다 평균 2.42매씩 늘어납니다
- 단위를 실제 기기와 같은 **'매'** 로 셉니다. 1게임 3매 = 베팅 금액 한 번
- **200만 게임을 돌려 재서** 맞췄습니다. 잰 값은 그 파일 맨 아래 주석에 있습니다

| 잰 것 | 값 | 실제 기기 |
| --- | --- | --- |
| 환급률 | 95.03% | — |
| 당첨 간격 | 227게임 (통상 73.4% · 찬스존 25.8% · 천장 0.8%) | — |
| AT 한 번 | 113게임 · 272매 | — |
| AT 순증 | 2.42매/G | 2.5 |
| 통상 순감 | 1.40매/G | 1.5 (천엔 33회전) |

- 맞춘 방법: 순감·순증은 실제 값에 고정해 두고 **AT가 이어질 확률(`pachiAtContinue`)만**
  움직여 95%에 맞췄습니다. 이 값을 올리면 AT가 길어져 환급률이 같이 올라갑니다
- 화면 맨 위에 **지금 구간(통상/찬스존/AT) · 돌린 게임 수 · 천장까지 남은 수**를 띄우고
  천장 게이지를 그립니다. 이것이 없으면 천장이 있다는 걸 알 수가 없습니다
- 옛 로직(`src/slot.ts`의 `evaluatePachislot`)은 **지우지 않고 그대로 두었습니다**

### 0-5. 바카라 뱅커 자리 — 안 바꿉니다 (확인함)

"바카라만 뱅커가 아래에 있다"는 지적을 확인했습니다. 실제로 혼자 반대입니다.

| 게임 | 위 | 아래 |
| --- | --- | --- |
| 블랙잭 · 파이 고우 · 틴 파티 | 딜러 / 컴퓨터 | 나 |
| 바카라 | PLAYER | BANKER |

**하지만 실수가 아닙니다.** 블랙잭의 딜러는 내 상대지만, **바카라의 뱅커는 상대가 아니라
내가 걸 수 있는 두 쪽 중 하나**입니다. 뱅커에 걸면 뱅커가 내 편입니다. 실제 카지노도 두 패를
테이블 가운데에 나란히 깔지 뱅커를 딜러 자리에 두지 않습니다. **그대로 두기로 했습니다.**

### 1. 게임 다시 만들기 (8월 30일에 정해진 것)

**1-1. ~~물고기 룰렛~~ — 끝났습니다 (8월 30일)**

스크린낚시와 피시 레이스를 **둘 다 목록에서 빼고**(코드는 `src/screenfishing.ts` ·
`src/fishrace.ts`에 그대로 남아 있습니다) 그 자리에 `물고기 룰렛`을 새로 넣었습니다.

- 로직은 `src/fishroulette.ts`, 화면은 `FishRouletteGameScreen`입니다
- 둥근 바다 둘레에 자리 12곳, 물고기 12마리, 한 판 20초. 자리에 들어가면 다시 안 나오고
  20초 안에 못 들어간 물고기는 안 셉니다(평균 5.0%가 못 들어갑니다)
- 승식 여섯 가지 · **배당은 200만 판을 돌려 재서** 정했습니다. 잰 값은 그 파일 맨 아래 주석에 있습니다

| 승식 | 잰 확률 | 정한 배당 | 잰 환급률 |
| --- | --- | --- | --- |
| 한 자리 · 먼저 | 8.32% | 11.4배 | 94.8% |
| 한 자리 · 많이 | 8.32% | 11.4배 | 94.9% |
| 한 자리 · 없음 | 37.16% | 2.55배 | 94.8% |
| 이웃 두 자리 · 먼저 | 16.67% | 5.7배 | 95.0% |
| 홀 / 짝 · 먼저 | 50.04% | 1.9배 | 95.1% |
| 앞 절반 / 뒤 절반 · 먼저 | 49.97% | 1.9배 | 94.9% |

- **동점은 "같은 수면 먼저 들어간 쪽"으로 풉니다.** 그 덕에 동점이 아예 안 생기고,
  자리 12곳을 따로 세어 봐도 먼저·많이 모두 8.30~8.38%로 고르게 나옵니다
- "없음"이 처음 계산한 35.2%보다 높은 것은 못 들어가는 물고기 5%때문입니다.
  12마리가 다 들어간다면 (11/12)^12 = 35.2%가 됩니다
- **8월 30일에 원을 하나로 키웠습니다.** 예전에는 물고기가 자리 바깥(134)에 붙어서
  원이 둘로 보였습니다. 지금은 판을 320으로 키우고 자리를 가장자리(134)로 밀어낸 뒤,
  **물고기는 자리 안쪽(106)에 모입니다.** 물(`fishRouletteWater`)도 232로 키워
  자리 안쪽을 꽉 채웁니다
- ⚠️ **아직 원하시는 모양이 아닙니다.** 자리를 원 **바깥**에 두라는 뜻이었습니다.
  위 0-7을 보세요
- 헤엄치는 띠도 판 한가운데 글자를 안 가리게 바깥쪽으로 잡았습니다(`startRadius` 0.46~0.70)
- 아이폰(375×812)에서 스크롤 없이 들어갑니다. 버튼 아래가 802에서 끝납니다

**1-2. ~~그레이하운드 → 예측 마켓 둘로 나누기~~ — 끝났습니다 (8월 30일)**

그레이하운드를 목록에서 뺐고(코드는 `src/greyhound.ts`에 그대로 있습니다) 그 자리에
예측 마켓을 `예측 마켓 · 스포츠`(球)와 `예측 마켓 · 사회문제`(社) 두 게임으로 나눠 넣었습니다.

- 화면은 `PredictGameScreen` 하나를 그대로 쓰고 `group` 값을 받게만 바꿨습니다.
  갈래를 게임이 정하므로 **화면 안에 있던 갈래 탭은 없앴습니다**
- 기록에 남는 이름도 둘로 갈라져서 지갑의 게임별 손익이 따로 나옵니다
- 문제 수는 2026-08-30 기준 86개 — 스포츠 54, 사회문제 32. 매일 아침 6시에 새로 받아 옵니다
- 사회문제 32개는 한 게임으로 떼어 놓기에 적어서, `scripts/fetch-kalshi.mjs`가
  **시리즈 1400개를 훑어 80개까지 가져오도록** 늘렸습니다(전에는 700개를 훑어 40개).
  다음 아침 갱신부터 늘어납니다

**1-3. 빈 자리 하나는 그냥 둡니다**

지금 스포츠/아케이드는 **경마 · 경륜 · 예측 마켓 · 그레이하운드 · 물고기 룰렛** 다섯입니다.
1-2를 하고 나면 **경마 · 경륜 · 예측 마켓(스포츠) · 예측 마켓(사회문제) · 물고기 룰렛**
다섯이 됩니다. 한 자리가 비는데 **억지로 채우지 않습니다.** 나중에 하고 싶은 게 나오면 넣습니다.

나중에 쓸 후보(전부 경주가 아닌 것): 인형뽑기(크레인) · 두더지 잡기 · 소싸움 되살리기 ·
쉬운 로또 · 다트(조작이 겹침) · 파칭코(행운의 물고기와 구조가 같음).

### 2. 화면 고치기

**2-1. ~~경륜~~ — 끝났습니다**

- 트랙을 **세로로 긴 타원**(344×424)으로 만들었습니다. 한 바퀴가 길어져 경기를 오래 봅니다
- 작게 만든 것은 트랙이 아니라 **선수**입니다(30 → 20). 경기 중에는 번호를 빼고 색만 남겼습니다.
  번호는 아래 순위줄에서 봅니다

**2-2. ~~공 어디에?~~ — 접었습니다**

- **컵마다 색이 달라서 공이 든 컵을 색으로 따라가면 그만이었습니다.** 처음에 공을 보여줄 때
  그 색만 기억하면 100% 맞혔고, 배당이 3배라 환급률이 300%였습니다
- 실제 야바위처럼 세 컵을 똑같이 만들어 봤지만 그러면 아무것도 안 보인다고 하셔서 **목록에서 뺐습니다**.
  코드는 그대로 있습니다(`src/shellgame.ts`, `ShellGameScreen`)

**2-3. 고스톱 — 8월 30일에 많이 고쳤습니다**

끝난 것:

- **치는 모습을 보여 줍니다.** 낸 패를 바닥의 맞은 패 위에 비스듬히 얹고, 1.28배로 커졌다
  0.26초 뒤 돌아옵니다. 그 옆에 더미에서 깐 패와 그것이 친 바닥 패도 같이 보여 줍니다.
  **이번에 가져가는 패에만 금테**를 둘러 무엇이 내 것이 되는지 알려 줍니다
  - 계산은 미리 끝내 두고(`slap.next`) 화면만 두 걸음으로 나눈 것이라 판정은 그대로입니다
  - 금테는 **치는 순간 위에서만** 보입니다. 아래 쌓인 패에도 두르면 판이 끝난 뒤까지 남아 지저분합니다
- **속도** — 컴퓨터가 0.7초 뒤 저절로 내고, 친 패를 1.2초 보여 준 뒤 가져갑니다. 한 사람당 2초쯤입니다.
  누를 것은 내 패 고르기와 고·스톱뿐입니다
- **내 차례에 칠 수 있는 패**를 12만큼 들어 올리고 금빛을 둘렀습니다
- **자리** — 내 자리를 맨 아래에 붙이고 컴퓨터 자리를 키웠습니다(최소 높이 130).
  모은 패는 28×42로 키우고 줄바꿈을 허용했습니다
- **화투패 그림**을 새 그림 48장으로 바꿨습니다(끝난 것 절 참고)

남은 것:

- **모은 패 무리 자리가 아직 흔들립니다.** 광·열끗·띠·피 네 무리로 나눠 놓았는데,
  어느 무리가 비면 그만큼 자리가 당겨져서 위치가 바뀝니다. **비어도 자리를 비워 두어야** 합니다
- **판 가운데가 넓게 빕니다.** `goStopFloorArea`가 남는 자리를 나눠 갖습니다
- ⚠️ **"공부해와"** — 실제 고스톱 앱들이 모은 패를 어떻게 늘어놓는지 먼저 보고 오라는 뜻입니다

### 3. 카드 크기 정하기

- **카드 크기는 게임마다 다르게 하지 말고 하나로 통일합니다**
- 다만 **마작·고스톱처럼 패가 많아 작게 넣은 것은 오히려 조금 크게** 합니다.
  작아서 안 보이는 게 더 문제입니다
- **자리에 맞춰 크기를 자동으로 고르는 것은 시험해 볼 만합니다.** 판이 쓸 수 있는 높이를
  재서(`onLayout`) 몇 단계 중 하나를 고르는 방식입니다. 자리가 모자라면 작은 단계로 내려가
  버튼이 밀리지 않고, 남으면 큰 단계로 올라가 빈자리가 안 생깁니다
- **시험은 파이 고우부터** 하는 게 좋습니다. 일곱 장이라 지금도 제일 빠듯해서
  되는지 안 되는지가 제일 빨리 드러납니다

⚠️ **왜 이게 중요한가** — 지금까지 실제로 터진 문제는 **전부 "자리가 모자람"** 이었습니다.
파이 고우는 카드가 두 줄로 접혀 버튼이 화면 밖으로 밀렸고, 블랙잭은 결과가 나오면
`같은 금액으로 다시 하기`가 잘렸습니다. 마작 네 종류·차이니즈 포커·빅투·조커 포커는
패가 13~14장이라 아예 손도 못 댔습니다. **"남을 때 어떻게 쓸까"는 드문 쪽입니다.**

### 4. 남은 것 (앞에서 이어짐)

- **한 화면에 고정 — 남은 네 화면**: 마작 4종 · `ChinesePokerGameScreen` ·
  `BigTwoGameScreen` · `JokerPokerGameScreen`. 3번(카드 크기 자동)이 되면 같이 풀립니다
- **1대 다수** — 손님은 배경 인형이 아니라 실제로 판단하는 상대여야 합니다.
  고스톱·맞고는 이미 됩니다(`playerCount`). 마작도 이미 4명입니다(컴퓨터 3).
  **8월 30일에 두 갈래로 갈랐습니다**(위 0-0의 1번):
  - **뱅커 카드** — 반원형 `DealerTable` 유지. 바카라(제일 쉬움) → 블랙잭 → 파이 고우
  - **개인 카드** — 네모 테이블. 세븐 포커 · 하이로우 → 홀덤 · 오마하 (위 0-2)
- **배경 사진** — 첫 페이지는 건드릴 필요 없습니다. 슬롯·카지노 게임·카드 게임 뒤에 깔
  사진을 정해야 하고, **파일을 `assets/`에 넣어 주셔야 합니다**(대화에 올린 사진은 저장소에 못 넣습니다)
- **움직임 다듬기** — 윷 던지는 느낌, 비디오포커 카드 회전

---

## 지금 화면이 남는 자리를 어떻게 쓰는지

막대를 줄여서 세로로 25~27픽셀을 되찾았습니다. 그 자리를 화면마다 다르게 쓰고 있습니다.
**카드나 그림이 늘어나는 곳은 없습니다. 늘어나는 건 여백뿐입니다.**

| 방식 | 화면 | 어떻게 되나 |
| --- | --- | --- |
| 판이 커지고 줄 사이가 벌어짐 (`flex: 1` + `space-evenly`) | 세븐 포커 · 하이로우 · 홀덤 · 오마하 · 섰다 · 도리짓고땡 | 초록 판이 커지고 카드 줄이 서로 벌어집니다 |
| 판은 제 크기, 판과 버튼 사이가 벌어짐 | 블랙잭 · 바카라 · 파이 고우 · 틴 파티 · 비디오 포커 | 반원 테이블은 그대로, 아래에 빈자리가 생깁니다 |
| 가운데만 벌어짐 | 고스톱 · 맞고 | 상대 자리와 손패는 붙어 있고 바닥만 넓어집니다 |

3번(카드 크기 자동)을 하면 이 셋을 하나로 합칠 수 있습니다.

---

## 어디를 고치면 되는지

`App.tsx`가 6,800줄이 넘어 매번 찾아 헤매면 시간이 많이 듭니다. 줄 번호는 금방 어긋나므로
**함수 이름과 스타일 이름**으로 적어 둡니다. 이 이름으로 검색하면 바로 나옵니다.

| 할 일 | 화면 함수 | 손댈 스타일 · 값 |
| --- | --- | --- |
| 물고기 룰렛 | `FishRouletteGameScreen` | `fishRouletteBoard` 320 · `fishRouletteRing` 134(자리) · `fishRouletteFishRing` 106(물고기, **자리 안쪽**) · 물 `fishRouletteWater` 232, 로직은 `src/fishroulette.ts` |
| 개인 카드 인원 | `PlayerCountRow` · 상태는 `tablePlayers` | 베팅 한 라운드는 `src/table.ts`(`applyTableAction`) · 자리 줄은 `tableSeatRow` · 상대 카드 크기는 `tinyPlayingCard` |
| 개인 카드 화면(0-6) | 세 화면의 `seatRow` | 눕힌 카드는 `PlayingCard`의 `sideways`(`sidewaysCard` · `tableSideCardBox` 66×44 · `tableSideCardStack` −22) · 안 움직이게 잡은 값은 `tableSideSlotTall` 210 · `tableMyCardsSeven` 302 · `tableFrontRow` 56 · `tableBottomSlot` 52 · `tableChipPile` 29 · 행동 표시는 `useSeatActions`(1.6초) · 칩은 `SeatChips` |
| 파치슬롯 | `PachislotGameScreen` | 로직은 `src/pachislot.ts`(천장 `pachiCeiling` · 찬스존 `pachiZoneEvery` · 환급률은 `pachiAtContinue`로 맞춤) · 상태판은 `pachiStatusPanel` |
| 예측 마켓 두 갈래 | `PredictGameScreen`(`group`으로 갈래를 받음) | 로직은 `src/predict.ts`의 `predictGroupOf` · 문제는 `src/predictdata.ts` |
| 경륜 원 줄이기 | `Velodrome` | `velodromeSize` 268 · `velodromeLaneOuter` 112 · `velodromeLaneInner` 84 · `velodromeRiderText`(숫자) |
| 공 어디에 | `ShellGameScreen`, `src/shellgame.ts` | 컵 움직임 |
| 고스톱 | `GoStopGameScreen` | 상대 속도는 `setTimeout(...,800)` · 모은 패는 `takenRow`와 `goStopTakenOrder` · `goStopTakenRow` |
| 카드 크기 | `PlayingCard`(공용) | `playingCard` 72×108 · `compactPlayingCard` 58×88 · `dealerCardFan` −30 · `sevenPokerCardOverlap` −22 |
| 화투패 크기 | `HwatuCardView`(`size` 로 골라 씀) | `hwatuCard` 52×78 · `hwatuCardSmall` 40×60 · `hwatuCardTiny` 20×30 |
| 1대 다수 | `BaccaratGameScreen` → `BlackjackGameScreen` → `PaiGowGameScreen` → `PokerGameScreen` | `DealerTable` |
| 배경 사진 | 화면별 바탕 | `app` · `detailScreen` · `pokerTable` `#052E22` · `baccaratScreen` `#071D25` · `goStopBoard` `#0E5636` · `fishingScreen` `#06202E` |
| 윷 던지는 느낌 | `YutBetGameScreen`, `YutStickView` | `yutStick` · `yutStickTumbling` · `yutMat` |
| 비디오포커 회전 | `VideoPokerGameScreen` | `videoPokerHand` · `videoPokerCardWrap` |

### 한 화면에 고정하는 법

바깥을 `styles.fixedTableArea`(`flex: 1` · `space-between`)로 감쌉니다. 판에 원래 걸려 있던
`minHeight`(510 · 520 · 570)를 풀어야 아래가 안 밀립니다. 패가 많으면 부채처럼 겹칩니다
(`dealerCardFan` · `sevenPokerCardOverlap`). **이긴 카드는 위로 16 들리므로** 카드 줄에
`paddingTop: 16`을 줘야 바로 위 이름줄을 안 덮습니다.

자리가 모자랄 때 무엇이 먼저 양보하는지 정해 둬야 합니다. 지금은 반원 테이블이
`flexShrink: 1` + `overflow: hidden`이라 테이블 아래가 잘리고 버튼은 남습니다.
**버튼이 밀려 화면 밖으로 나가는 것보다 이쪽이 낫습니다.**

### 1대 다수는 화면과 로직을 둘 다 고쳐야 합니다

`DealerTable`은 안에 넣은 것을 세로로 쌓기만 하므로 손님을 더 넣으면 원 둘레에 앉는 게
아니라 줄만 늘어납니다. 로직도 손님 한 명 기준입니다 — `dealInitialRound`는 `player` 하나와
`dealer` 하나만 돌리고, `dealPaiGow`는 덱에서 7장씩 딱 두 몫만 뗍니다. 손님 수를 인자로 받아
몫을 나누도록 바꿔야 합니다.

**바카라만 예외로 쉽습니다.** 손님이 몇 명이든 PLAYER·BANKER 패 한 벌을 같이 보므로
카드 로직은 그대로 두고 베팅만 늘리면 됩니다.

### 한 장씩 공개를 다른 게임에 더 넣을 때

공용 도구가 이미 있습니다. `useReveal()`(연 장수 세기)와
`<RevealButton opened total onPress label/>`. `BaccaratGameScreen`이나 `TujeonGameScreen`을
보고 따라 하면 됩니다. 핵심은 **정산(`onSettle`)을 마지막 장이 열린 뒤로 미루는 것**입니다.

### 새 게임 붙이는 자리 (다섯 군데를 다 고쳐야 합니다)

1. `src/<이름>.ts` — 판정 로직 (반드시 `.ts`, `.tsx` 아님)
2. `tests/<이름>.test.ts` + `package.json`의 `test` 스크립트에 파일 이름 추가
3. `App.tsx`의 `gameCategories` — 목록에 한 줄
4. `App.tsx`의 `AppScreen` 타입, `gameEntryScreens`, `settleNewGame`의 게임 이름 목록
5. `App.tsx`에 `<이름>SetupScreen`(대개 `SimpleSetupScreen` 재사용)과 `<이름>GameScreen` 렌더 줄

---

## 지금 상태

- 게임 **32개**, 전부 플레이 가능 (그레이하운드를 빼고 예측 마켓을 둘로 나눴습니다)
- 테스트 **643개 통과**, 타입 오류 0, 웹 빌드 확인됨
- 개인 카드 여섯 게임은 **인원 2~4명**을 고를 수 있습니다(위 0-2)
- 어디까지 했는지는 `git log --oneline -5`로 봅니다

| 분류 | 게임 |
| --- | --- |
| 개인 카드 | 텍사스 홀덤 · 오마하 · 세븐 포커 · 하이로우 · 차이니즈 포커 · 빅투 |
| 주사위 | 야찌 · 크랩스 · 식보 · 물고기 룰렛 (4개 · 두 자리 빔) |
| 뱅커 카드 | 블랙잭 · 바카라 · 파이 고우 · 틴 파티 · 비디오 포커 · 조커 포커 |
| 자동 배팅 | 슬롯 · 룰렛 · 경마 · 경륜 · 예측 마켓(스포츠) · 예측 마켓(사회문제) |
| 한국 전통 | 고스톱 · 맞고 · 섰다 · 도리짓고땡 · 투전 · 윷 베팅 |
| 마작 | 리치 · 중국식 · 홍콩 · 사천 (4개 · 두 자리 빔) |

---

## 8월 29~30일에 한 것

**카드 게임 열세 화면을 한 화면에 고정** — 블랙잭 · 고스톱 · 맞고 · 세븐 포커 · 하이로우 ·
바카라 · 파이 고우 · 틴 파티 · 비디오 포커 · 섰다 · 도리짓고땡 · 홀덤 · 오마하.
아이폰(375×812)에서 스크롤 없이 맨 아래 글이 802에서 끝납니다.

**반원형 딜러 테이블** — 블랙잭 · 바카라 · 파이 고우 · 틴 파티. 딜러가 있는 게임에는
슈·버림 통·칩 트레이를, 컴퓨터가 상대인 게임에는 상대 좌석과 카드 더미만 놓습니다.

**고스톱 판을 실제 판처럼** — 위가 상대, 가운데가 바닥과 더미, 아래가 내 자리와 손패.
판 전체가 초록입니다. 상대 손패는 뒷면 장수만, 모은 패는 아주 작은 패로 겹쳐 쌓습니다.
컴퓨터는 0.8초씩 쉬며 한 명씩 둡니다.

**스크린낚시 추가** — 행운의 물고기 자리(코드는 살아 있음). 배당은 자리마다 40만 판씩
돌려 재고 맞췄습니다. 챔질 90%면 94~97%, 100%면 105~108%, 70%면 74% 안팎.
**단, 이 방식은 접기로 했습니다**(위 1-1).

**화투패 48장을 새 그림으로** — 받은 그림으로 전부 바꿨습니다.
**자르지 않고 통째로 줄여** 넣고 남는 자리는 투명하게 뒀습니다. 그래서 48장이 전부 **150×225**로
같습니다. 예전에는 372×558 · 336×504 · 348×522처럼 제각각이었는데, 제가 그림마다 여백을
잘라냈기 때문입니다. 나란히 놓았을 때 지저분해 보이던 원인이 그것이었습니다.

- 150×225인 이유: 화면에 52×78로 그리는데 아이폰이 점을 3배로 찍어 156×234가 필요합니다.
  원본이 130×205 안팎이라 더 키워 봐야 뭉개지기만 합니다
- 8월 30일에 **피 14장을 다시 받아 넣었습니다**(1·2·3·4·5·8·10월 두 장씩).
  6·7·9·11·12월 피는 안 주셔서 그대로입니다. 받은 폴더는 `C:/Users/aaspz/Desktop/고스톱 피`
- **11월 피 두 장은 거꾸로 들어와서 180도 돌렸습니다.** 새 그림을 받으면 방향도 꼭 확인하세요
- ⚠️ **`scripts/trim-hwatu.mjs`를 다시 돌리지 마세요.** 여백을 또 잘라내서 크기가 제각각이 됩니다
- 새 그림을 넣을 때는 `C:/Users/aaspz/AppData/Local/Temp/claude/pdftool/`의 `convert.mjs`(전체) ·
  `pi.mjs`(피만) · `sheet.mjs`(여러 장을 한 장에 모아 붙여 눈으로 확인) · `rotate.mjs`(180도)를
  본보기로 쓰세요. 월별 자리 순서는 `src/hwatu.ts`의 `monthSpecs` 그대로입니다
  (0번이 광 또는 열끗, 1번이 띠, 2·3번이 피)

**경륜 원형 벨로드롬** · **홀덤·오마하 한 장씩 열기**(이로써 한 장씩 열기 전부 완료).

**막대 정리** — 위 막대 68 → 40(안의 동그라미 38→30, 코인 알약 40→32도 같이 줄여야
낮아집니다), 아래 탭바 72 → 52에 버튼이 막대를 꽉 채웁니다. 게임 중에는 위 막대가
아예 안 나옵니다(탭 화면에서만 보임).

### 오래 헤맨 것 — 시계 아래 가짜 빈 띠

`public/index.html`이 상태바를 `black`(불투명)으로 두어서 **iOS가 이미 시계 아래에서부터
화면을 시작해 줍니다.** 그래서 `env(safe-area-inset-top)`이 0으로 나오는 게 정상인데,
코드가 그걸 "값을 못 읽었다"로 보고 44를 강제로 넣고 있었습니다. 그 44가 통째로 빈 띠였습니다.
강제값을 없애고 읽은 값을 그대로 씁니다.

시계·배터리는 iOS가 그리는 것이라 **웹 앱이 끌 수 없습니다.** `black-translucent`로 바꾸면
화면이 맨 위까지 올라가지만 시계가 앱 위에 겹쳐 그려집니다.

### 화면 판번호 — "안 바뀐 것 같다"는 말이 나오면 이것부터

설정 맨 아래에 배포 날짜와 커밋이 찍힙니다(`EXPO_PUBLIC_BUILD`, 배포 워크플로에서 넣음).
아이폰 홈 화면 앱이 옛 파일을 오래 들고 있어서, 이번에 **몇 번이나 엉뚱한 곳을 고쳤습니다.**
판번호가 다르면 폰이 옛 화면을 들고 있는 것이고, 같은데도 안 바뀌었으면 그때가 진짜 버그입니다.

폰에서 새로 받는 법: 홈 화면 앱을 완전히 닫았다 열기 → 안 되면 사파리에서 새로고침 →
그래도 안 되면 아이콘 지우고 다시 추가.

### 그 밖에 고친 것

- 진 쪽 카드가 안 보이던 것 — `cardDim` 투명도 0.04 → 0.4
- 블랙잭 결과 상자가 화면 밖으로 밀리던 것 — `카지노 목록으로`(위 막대 `나가기`와 중복)를 뺌
- 바카라 베팅 자리를 펠트 안으로 옮김. 반원 곡선에 잘려서 줄 폭을 256 → 214로 좁힘
- 배포 워크플로가 `App.tsx`의 탭바 한 줄을 문자열로 바꾸던 것을 없앰(그 줄을 고치면
  빌드가 멈췄습니다). 웹 전용 위치 지정은 이제 소스 안에 있습니다

---

## 미해결

**아이폰에서 하단 바가 다시 올라옴** — 전체화면으로 쓰다가 다른 앱을 보고 돌아오면 탭바 아래에
빈 띠가 생깁니다. 지금 처리는 여백을 없앤 게 아니라 **같은 색으로 가린 것**입니다
(`public/index.html`의 배경색). 실기기에서만 재현되어 여기서 검증할 수 없습니다.

---

## 작업 규칙

- **모르면 묻습니다. 정해 준 범위 밖으로 나가지 않습니다.** 여러 대상을 말했을 때
  어느 것인지 확인하고 시작합니다
- 카테고리마다 게임 6개를 넘지 않습니다
- **목록에서 뺀 게임의 코드는 지우지 않습니다.** `gameCategories`에서만 뺍니다.
  한 줄 넣으면 되살아납니다
- 판정 로직은 반드시 `src/*.ts`에 둡니다. 테스트는 `.ts`만 읽을 수 있어 JSX가 든 `.tsx`는 못 읽습니다
- 새 게임을 만들면 `package.json`의 `test` 스크립트에 테스트 파일을 **직접 추가**해야 합니다
- **목록에만 넣고 화면을 안 만들면 시작할 때 오류 화면이 뜹니다**
- 배당은 **짐작하지 말고 시뮬레이션으로 재서** 정합니다. 잰 값은 코드 주석에 남깁니다
- **이겼는데 손해 보는 배당을 두지 않습니다**(1배 미만). 예측 마켓에서 한 번 터진 적 있습니다
- 다 하지 않고 "완성했다"고 말하지 말 것

### 이 PC에서 작업할 때

- Node는 winget으로 설치한 24.19.0인데 셸 PATH에 안 잡힙니다.
  명령 앞에 `export PATH="/c/Program Files/nodejs:$PATH"`를 붙이세요
- `node_modules`는 pnpm 설치본입니다. `npm install`은 이 저장소에서 실패하니, 도구가 필요하면
  저장소 밖 폴더에 따로 설치해 경로로 부르세요(`scripts/trim-hwatu.mjs`가 그 방식입니다)
- **커밋 전에 `git fetch origin`으로 원격을 확인하세요.** GitHub 웹에서 직접 올리는 경우가 있습니다
- 셸에서 백틱이 든 문자열은 먹힙니다. 여러 줄을 고칠 때는 스크래치 폴더에 `.mjs` 파일을 쓰고
  `node`로 돌리는 편이 안전합니다

## 문제 데이터 갱신 (예측 마켓)

`.github/workflows/fetch-kalshi.yml`이 매일 한국 시간 아침 6시에 kalshi.com에서 받아
`src/predictdata.ts`로 커밋합니다. 수동 실행도 됩니다. 앱은 그 파일만 읽습니다
(kalshi가 CORS를 안 열어 브라우저에서 직접 못 부릅니다).

## 목록에서 빠졌지만 코드가 살아 있는 게임

한 줄만 넣으면 되살아납니다.

| 게임 | 코드 |
| --- | --- |
| 그레이하운드 | `src/greyhound.ts` · `GreyhoundRacingGameScreen` |
| 육백 | `src/yukbaek.ts` |
| 자동차 레이스 | `src/carracing.ts` |
| 소싸움 | `src/bullfighting.ts` |
| 홀짝 | `oddEvenSetup`/`oddEvenGame` |
| 파이브 카드 드로우 | `src/fivecarddraw.ts` |
| 민화투 | `src/minhwatu.ts` |
| 로또 | `src/worldgames.ts` |
| 즉석 복권 | `ScratchGameScreen` |
| 경정 | `src/boatracing.ts` |
| 코인 푸셔 | `src/coinpusher.ts` |
| 행운의 물고기 | `src/luckyfish.ts` |
| 스크린낚시 | `src/screenfishing.ts` · `ScreenFishingGameScreen` |
| 피시 레이스 | `src/fishrace.ts` · `FishRaceGameScreen` |

## 손 안 댄 것

**`README.md`가 오래됐습니다.** 한국 전통이 "준비 중"이라고 되어 있고, 목록에서 빠진 게임이
그대로 있습니다. 게임 작업이 끝난 뒤 한 번에 고치는 편이 낫습니다.
