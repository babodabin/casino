# 다음에 할 일 (2026-08-30 갱신)

저장소: https://github.com/babodabin/casino · 배포: https://babodabin.github.io/casino
main에 푸시하면 GitHub Actions가 자동 배포합니다. **푸시 = 라이브 배포**입니다.

**이 문서 하나만 읽으면 바로 시작할 수 있게 적었습니다.** 코드를 뒤져 상황을 파악할
필요가 없도록, 어디를 고치는지와 왜 그렇게 되어 있는지를 같이 적어 두었습니다.

---

## 바로 시작할 것

8월 30일에 받은 지시입니다. 위에서부터가 급한 순서지만 순서는 바꿔도 됩니다.

### 1. 게임 다시 만들기 (8월 30일에 정해진 것)

**1-1. 물고기 룰렛 — 스크린낚시와 피시 레이스를 합쳐서 새로 만듭니다**

지금 스크린낚시(던지기 → 챔질 → 릴 싸움)는 **접습니다.** 직접 해 보시고 별로라고 하셨습니다.
어제 피시 레이스를 원형으로 바꾸자던 것과 같은 게임이라 **둘을 합칩니다.**

- **이름은 `물고기 룰렛`**. 스크린낚시 자리에 넣고, **피시 레이스는 목록에서 뺍니다**(코드는 남김)
- **둥근 바다 = 룰렛판.** 바깥 둘레에 번호 자리 **12곳**
- 시작하면 **물고기 12마리**를 풀어 놓습니다. 헤엄쳐 다니다 바깥 자리로 들어갑니다
- **자리에 들어가면 거기서 끝**입니다. 다시 안 나옵니다
- **시간이 다 되면 판이 끝납니다.** 그때까지 못 들어간 물고기는 안 셉니다
- 보는 재미가 핵심입니다. 12마리가 각자 헤엄쳐 들어가는 걸 지켜봅니다

승식(룰렛처럼 여러 가지):

| 승식 | 맞을 확률 | 배당(환급률 95% 기준) |
| --- | --- | --- |
| 한 자리 · **먼저** | 8.3% (1/12) | 약 11배 |
| 한 자리 · **많이** | 8.3% (1/12) | 약 11배 |
| 한 자리 · **없음** | 35.2% | 약 2.6배 |
| 이웃 두 자리 · 먼저 | 16.7% | 약 5.6배 |
| 홀 / 짝 · 먼저 | 50% | 약 1.9배 |
| 앞 절반(1~6) / 뒤 절반(7~12) · 먼저 | 50% | 약 1.9배 |

⚠️ **동점 규칙 — "많이"에서 꼭 필요합니다.** 자리 12곳에 물고기 12마리면 한 자리에 평균 한 마리라,
제일 많이 들어간 자리가 2마리인데 그런 자리가 서너 곳 나오는 일이 흔합니다.
**같은 수면 먼저 들어간 쪽이 이깁니다.** 이러면 동점이 아예 안 생기고, 대칭이라 각 자리의 확률이
정확히 1/12로 떨어집니다. 위 표가 그 전제로 계산한 값입니다.

- 물고기를 늘리면 "없음"이 드물어져 배당이 커지고(20마리면 17%), 줄이면 "많이"가 심심해집니다.
  **12마리가 균형이 좋습니다**
- 배당은 표를 그대로 쓰지 말고 **시뮬레이션으로 재서** 확정합니다. 헤엄치는 방식에 따라 달라집니다
- 지금 코드: `src/screenfishing.ts`(물고기 표·배당은 안 쓰게 되면 지우지 말고 남김) ·
  `src/fishrace.ts` · 화면은 `ScreenFishingGameScreen` · `FishRaceGameScreen`
- 원 위에 위치를 잡는 방법은 `Velodrome` 컴포넌트가 본보기입니다

**1-2. 그레이하운드 → 예측 마켓을 둘로 나눕니다**

경마와 너무 비슷해서 뺍니다. 코드는 남깁니다(`src/greyhound.ts`).
그 자리에 **예측 마켓을 `예측 마켓 · 스포츠`와 `예측 마켓 · 사회문제` 두 게임으로 나눠** 넣습니다.

- **문제 수는 충분합니다.** 2026-08-30 기준 86개 — 스포츠 54, 사회문제 32
  (경제 14 · 연예 6 · 과학기술 5 · 선거 5 · 정치 2). 매일 아침 6시에 새로 받아 옵니다
- 나누는 기준은 이미 있습니다: `src/predict.ts`의 `predictGroupOf`
- ⚠️ **사회문제가 32개뿐이라 며칠 하면 본 문제가 다시 나옵니다.** 필요하면
  `scripts/fetch-kalshi.mjs`에서 사회문제 쪽을 더 긁어 오게 늘리세요(지금은 700개를 훑어 40개만 가져옵니다)

**1-3. 빈 자리 하나는 그냥 둡니다**

합치고 나면 스포츠/아케이드가 **경마 · 경륜 · 예측 마켓(스포츠) · 예측 마켓(사회문제) · 물고기 룰렛**
다섯이 됩니다. 한 자리가 비는데 **억지로 채우지 않습니다.** 나중에 하고 싶은 게 나오면 넣습니다.

나중에 쓸 후보(전부 경주가 아닌 것): 인형뽑기(크레인) · 두더지 잡기 · 소싸움 되살리기 ·
쉬운 로또 · 다트(조작이 겹침) · 파칭코(행운의 물고기와 구조가 같음).

### 2. 화면 고치기

**2-1. 경륜 — 원을 줄이고 숫자를 뺍니다**

- 아래 순위 줄에 있는 **번호와 색은 지금 그대로 좋습니다**
- **경기 원 안에서는 선수 위에 적힌 숫자를 뺍니다.** 색 동그라미만 남깁니다
- **원을 지금보다 작게** 만듭니다. 그래야 선수 사이 간격이 좁아져서
  누가 먼저 들어오는지 더 잘 보입니다
- 화면 함수 `Velodrome`, 값은 `velodromeSize`(지금 268) · `velodromeLaneOuter`(112) ·
  `velodromeLaneInner`(84) · `velodromeRiderText`(이 글자를 빼면 됩니다)

**2-2. 공 어디에? — 두 가지 문제**

- **공이 항상 같은 색 자리에 있는지 확인이 필요합니다.** `src/shellgame.ts`를 읽고,
  컵 색과 공 위치가 상관없는지 봐야 합니다. 상관있으면 그건 버그입니다
- **2D도 아니고 1D처럼 보입니다.** 컵이 그냥 좌우로만 움직여서 진짜 섞이는 느낌이 안 납니다.
  컵이 서로 앞뒤로 지나가거나 위로 들리는 움직임이 있어야 합니다

**2-3. 고스톱 — 세 가지. 시작 전에 공부가 필요합니다**

- **상대가 두는 속도가 너무 빠릅니다.** 지금 0.8초입니다(`ScreenFishingGameScreen` 아님,
  `GoStopGameScreen`의 `useEffect` 안 `setTimeout(...,800)`). 늘리고, 무엇을 냈는지
  잠깐 보여 주는 표시도 필요합니다
- **내가 모은 패를 정리해 두는 자리가 판마다 조금씩 달라집니다.** 지금은 광·열끗·띠·피
  네 무리로 나누는데, 어느 무리가 비면 그만큼 자리가 당겨져서 위치가 흔들립니다.
  **무리마다 자리를 고정해 두어야 합니다**(비어도 자리를 비워 둠)
- ~~**화투패 그림**~~ — **끝났습니다.** 받은 그림으로 48장을 전부 바꿨습니다
  - **자르지 않고 줄여서** 통째로 넣고 남는 자리는 투명하게 뒀습니다. 48장이 전부 **150×225**로 같습니다
    (예전에는 372×558 · 336×504 · 348×522처럼 제각각이었습니다. 제가 한 장씩 잘라냈기 때문입니다)
  - 150×225로 정한 이유: 화면에 52×78로 그리는데 아이폰이 점을 3배로 찍어 156×234가 필요합니다.
    원본이 130×205 안팎이라 더 키우면 뭉개지기만 합니다
  - ⚠️ **`scripts/trim-hwatu.mjs`를 다시 돌리지 마세요.** 그걸 돌리면 여백을 또 잘라내서
    크기가 다시 제각각이 됩니다. 파일 맨 위에 경고를 적어 뒀습니다
  - 새 그림으로 또 바꿀 때는 `C:/Users/aaspz/AppData/Local/Temp/claude/pdftool/convert.mjs`가 본보기입니다
    (월별 자리 순서는 `src/hwatu.ts`의 `monthSpecs` 그대로: 0번이 광 또는 열끗, 1번이 띠, 2·3번이 피)
- ⚠️ **"공부해와"** — 실제 고스톱 앱들이 모은 패를 어떻게 늘어놓는지, 상대 차례를 어떻게
  보여 주는지 먼저 보고 오라는 뜻입니다. 바로 손대지 말고 확인부터 하세요

### 3. 카드 크기 정하기

- **카드 크기는 게임마다 다르게 하지 말고 하나로 통일합니다**
- 다만 **마작·고스톱처럼 패가 많아 작게 넣은 것은 오히려 조금 크게** 합니다.
  작아서 안 보이는 게 더 문제입니다
- **자리에 맞춰 크기를 자동으로 고르는 것은 시험해 볼 만합니다.** 판이 쓸 수 있는 높이를
  재서(`onLayout`) 몇 단계 중 하나를 고르는 방식입니다. 자리가 모자라면 작은 단계로 내려가
  버튼이 밀리지 않고, 남으면 큰 단계로 올라가 빈자리가 안 생깁니다
- **시험은 파이 고우부터** 하는 게 좋습니다. 일곱 장이라 지금도 제일 빠듯해서
  되는지 안 되는지가 제일 빨리 드러납니다

⚠️ **왜 이게 중요한가** — 지금까지 실제로 터진 문제는 **전부 "자리가 모자람"** 이었습니다.
파이 고우는 카드가 두 줄로 접혀 버튼이 화면 밖으로 밀렸고, 블랙잭은 결과가 나오면
`같은 금액으로 다시 하기`가 잘렸습니다. 마작 네 종류·차이니즈 포커·빅투·조커 포커는
패가 13~14장이라 아예 손도 못 댔습니다. **"남을 때 어떻게 쓸까"는 드문 쪽입니다.**

### 4. 남은 것 (앞에서 이어짐)

- **한 화면에 고정 — 남은 네 화면**: 마작 4종 · `ChinesePokerGameScreen` ·
  `BigTwoGameScreen` · `JokerPokerGameScreen`. 3번(카드 크기 자동)이 되면 같이 풀립니다
- **1대 다수** — 손님은 배경 인형이 아니라 실제로 판단하는 상대여야 합니다.
  고스톱·맞고는 이미 됩니다(`playerCount`). 남은 순서는
  바카라(제일 쉬움) → 블랙잭 → 파이 고우 → 홀덤·오마하(제일 큼)
  - **반원형은 손님이 늘어 안 들어가면 포기하고 세로 배치로 바꿉니다.** 보기 편한 쪽이 우선
- **배경 사진** — 첫 페이지는 건드릴 필요 없습니다. 슬롯·카지노 게임·카드 게임 뒤에 깔
  사진을 정해야 하고, **파일을 `assets/`에 넣어 주셔야 합니다**(대화에 올린 사진은 저장소에 못 넣습니다)
- **움직임 다듬기** — 윷 던지는 느낌, 비디오포커 카드 회전

---

## 지금 화면이 남는 자리를 어떻게 쓰는지

막대를 줄여서 세로로 25~27픽셀을 되찾았습니다. 그 자리를 화면마다 다르게 쓰고 있습니다.
**카드나 그림이 늘어나는 곳은 없습니다. 늘어나는 건 여백뿐입니다.**

| 방식 | 화면 | 어떻게 되나 |
| --- | --- | --- |
| 판이 커지고 줄 사이가 벌어짐 (`flex: 1` + `space-evenly`) | 세븐 포커 · 하이로우 · 홀덤 · 오마하 · 섰다 · 도리짓고땡 · 스크린낚시 | 초록 판이 커지고 카드 줄이 서로 벌어집니다 |
| 판은 제 크기, 판과 버튼 사이가 벌어짐 | 블랙잭 · 바카라 · 파이 고우 · 틴 파티 · 비디오 포커 | 반원 테이블은 그대로, 아래에 빈자리가 생깁니다 |
| 가운데만 벌어짐 | 고스톱 · 맞고 | 상대 자리와 손패는 붙어 있고 바닥만 넓어집니다 |

3번(카드 크기 자동)을 하면 이 셋을 하나로 합칠 수 있습니다.

---

## 어디를 고치면 되는지

`App.tsx`가 6,800줄이 넘어 매번 찾아 헤매면 시간이 많이 듭니다. 줄 번호는 금방 어긋나므로
**함수 이름과 스타일 이름**으로 적어 둡니다. 이 이름으로 검색하면 바로 나옵니다.

| 할 일 | 화면 함수 | 손댈 스타일 · 값 |
| --- | --- | --- |
| 스크린낚시 다시 | `ScreenFishingGameScreen` | `fishingSea` · `fishingStage` · `fishingWaves`, 로직은 `src/screenfishing.ts` |
| 피시 레이스 다시 | `FishRaceGameScreen` | `fishSwim` · `fishLane` · `fishRunner`, 로직은 `src/fishrace.ts`. 원 배치는 `Velodrome` 참고 |
| 그레이하운드 교체 | `gameCategories`의 스포츠/아케이드 줄 | 아래 "새 게임 붙이는 자리" |
| 경륜 원 줄이기 | `Velodrome` | `velodromeSize` 268 · `velodromeLaneOuter` 112 · `velodromeLaneInner` 84 · `velodromeRiderText`(숫자) |
| 공 어디에 | `ShellGameScreen`, `src/shellgame.ts` | 컵 움직임 |
| 고스톱 | `GoStopGameScreen` | 상대 속도는 `setTimeout(...,800)` · 모은 패는 `takenRow`와 `goStopTakenOrder` · `goStopTakenRow` |
| 카드 크기 | `PlayingCard`(공용) | `playingCard` 72×108 · `compactPlayingCard` 58×88 · `dealerCardFan` −30 · `sevenPokerCardOverlap` −22 |
| 화투패 크기 | `HwatuCardView`(`size` 로 골라 씀) | `hwatuCard` 52×78 · `hwatuCardSmall` 40×60 · `hwatuCardTiny` 20×30 |
| 1대 다수 | `BaccaratGameScreen` → `BlackjackGameScreen` → `PaiGowGameScreen` → `PokerGameScreen` | `DealerTable` |
| 배경 사진 | 화면별 바탕 | `app` · `detailScreen` · `pokerTable` `#052E22` · `baccaratScreen` `#071D25` · `goStopBoard` `#0E5636` · `fishingScreen` `#06202E` |
| 윷 던지는 느낌 | `YutBetGameScreen`, `YutStickView` | `yutStick` · `yutStickTumbling` · `yutMat` |
| 비디오포커 회전 | `VideoPokerGameScreen` | `videoPokerHand` · `videoPokerCardWrap` |

### 한 화면에 고정하는 법

바깥을 `styles.fixedTableArea`(`flex: 1` · `space-between`)로 감쌉니다. 판에 원래 걸려 있던
`minHeight`(510 · 520 · 570)를 풀어야 아래가 안 밀립니다. 패가 많으면 부채처럼 겹칩니다
(`dealerCardFan` · `sevenPokerCardOverlap`). **이긴 카드는 위로 16 들리므로** 카드 줄에
`paddingTop: 16`을 줘야 바로 위 이름줄을 안 덮습니다.

자리가 모자랄 때 무엇이 먼저 양보하는지 정해 둬야 합니다. 지금은 반원 테이블이
`flexShrink: 1` + `overflow: hidden`이라 테이블 아래가 잘리고 버튼은 남습니다.
**버튼이 밀려 화면 밖으로 나가는 것보다 이쪽이 낫습니다.**

### 1대 다수는 화면과 로직을 둘 다 고쳐야 합니다

`DealerTable`은 안에 넣은 것을 세로로 쌓기만 하므로 손님을 더 넣으면 원 둘레에 앉는 게
아니라 줄만 늘어납니다. 로직도 손님 한 명 기준입니다 — `dealInitialRound`는 `player` 하나와
`dealer` 하나만 돌리고, `dealPaiGow`는 덱에서 7장씩 딱 두 몫만 뗍니다. 손님 수를 인자로 받아
몫을 나누도록 바꿔야 합니다.

**바카라만 예외로 쉽습니다.** 손님이 몇 명이든 PLAYER·BANKER 패 한 벌을 같이 보므로
카드 로직은 그대로 두고 베팅만 늘리면 됩니다.

### 한 장씩 공개를 다른 게임에 더 넣을 때

공용 도구가 이미 있습니다. `useReveal()`(연 장수 세기)와
`<RevealButton opened total onPress label/>`. `BaccaratGameScreen`이나 `TujeonGameScreen`을
보고 따라 하면 됩니다. 핵심은 **정산(`onSettle`)을 마지막 장이 열린 뒤로 미루는 것**입니다.

### 새 게임 붙이는 자리 (다섯 군데를 다 고쳐야 합니다)

1. `src/<이름>.ts` — 판정 로직 (반드시 `.ts`, `.tsx` 아님)
2. `tests/<이름>.test.ts` + `package.json`의 `test` 스크립트에 파일 이름 추가
3. `App.tsx`의 `gameCategories` — 목록에 한 줄
4. `App.tsx`의 `AppScreen` 타입, `gameEntryScreens`, `settleNewGame`의 게임 이름 목록
5. `App.tsx`에 `<이름>SetupScreen`(대개 `SimpleSetupScreen` 재사용)과 `<이름>GameScreen` 렌더 줄

---

## 지금 상태

- 게임 **34개**, 전부 플레이 가능
- 테스트 **590개 통과**, 타입 오류 0, 웹 빌드 확인됨
- 어디까지 했는지는 `git log --oneline -5`로 봅니다

| 카테고리 | 게임 |
| --- | --- |
| 한국 전통 | 고스톱 · 맞고 · 섰다 · 도리짓고땡 · 투전 · 윷 베팅 |
| 카지노 | 블랙잭 · 바카라 · 룰렛 · 크랩스 · 식보 · 슬롯 |
| 포커·카드 | 텍사스 홀덤 · 오마하 · 세븐 포커 · 차이니즈 포커 · 비디오 포커 · 하이로우 |
| 마작 | 리치 · 중국식 · 홍콩 · 사천 (4개, 그대로 두기로 함) |
| 스포츠/아케이드 | 경마 · 경륜 · 예측 마켓 · 그레이하운드 · 피시 레이스 · 스크린낚시 |
| 세계 게임 | 야찌 · 파이 고우 · 틴 파티 · 빅투 · 조커 포커 · 공 어디에? |

---

## 8월 29~30일에 한 것

**카드 게임 열세 화면을 한 화면에 고정** — 블랙잭 · 고스톱 · 맞고 · 세븐 포커 · 하이로우 ·
바카라 · 파이 고우 · 틴 파티 · 비디오 포커 · 섰다 · 도리짓고땡 · 홀덤 · 오마하.
아이폰(375×812)에서 스크롤 없이 맨 아래 글이 802에서 끝납니다.

**반원형 딜러 테이블** — 블랙잭 · 바카라 · 파이 고우 · 틴 파티. 딜러가 있는 게임에는
슈·버림 통·칩 트레이를, 컴퓨터가 상대인 게임에는 상대 좌석과 카드 더미만 놓습니다.

**고스톱 판을 실제 판처럼** — 위가 상대, 가운데가 바닥과 더미, 아래가 내 자리와 손패.
판 전체가 초록입니다. 상대 손패는 뒷면 장수만, 모은 패는 아주 작은 패로 겹쳐 쌓습니다.
컴퓨터는 0.8초씩 쉬며 한 명씩 둡니다.

**스크린낚시 추가** — 행운의 물고기 자리(코드는 살아 있음). 배당은 자리마다 40만 판씩
돌려 재고 맞췄습니다. 챔질 90%면 94~97%, 100%면 105~108%, 70%면 74% 안팎.
**단, 이 방식은 접기로 했습니다**(위 1-1).

**경륜 원형 벨로드롬** · **홀덤·오마하 한 장씩 열기**(이로써 한 장씩 열기 전부 완료).

**막대 정리** — 위 막대 68 → 40(안의 동그라미 38→30, 코인 알약 40→32도 같이 줄여야
낮아집니다), 아래 탭바 72 → 52에 버튼이 막대를 꽉 채웁니다. 게임 중에는 위 막대가
아예 안 나옵니다(탭 화면에서만 보임).

### 오래 헤맨 것 — 시계 아래 가짜 빈 띠

`public/index.html`이 상태바를 `black`(불투명)으로 두어서 **iOS가 이미 시계 아래에서부터
화면을 시작해 줍니다.** 그래서 `env(safe-area-inset-top)`이 0으로 나오는 게 정상인데,
코드가 그걸 "값을 못 읽었다"로 보고 44를 강제로 넣고 있었습니다. 그 44가 통째로 빈 띠였습니다.
강제값을 없애고 읽은 값을 그대로 씁니다.

시계·배터리는 iOS가 그리는 것이라 **웹 앱이 끌 수 없습니다.** `black-translucent`로 바꾸면
화면이 맨 위까지 올라가지만 시계가 앱 위에 겹쳐 그려집니다.

### 화면 판번호 — "안 바뀐 것 같다"는 말이 나오면 이것부터

설정 맨 아래에 배포 날짜와 커밋이 찍힙니다(`EXPO_PUBLIC_BUILD`, 배포 워크플로에서 넣음).
아이폰 홈 화면 앱이 옛 파일을 오래 들고 있어서, 이번에 **몇 번이나 엉뚱한 곳을 고쳤습니다.**
판번호가 다르면 폰이 옛 화면을 들고 있는 것이고, 같은데도 안 바뀌었으면 그때가 진짜 버그입니다.

폰에서 새로 받는 법: 홈 화면 앱을 완전히 닫았다 열기 → 안 되면 사파리에서 새로고침 →
그래도 안 되면 아이콘 지우고 다시 추가.

### 그 밖에 고친 것

- 진 쪽 카드가 안 보이던 것 — `cardDim` 투명도 0.04 → 0.4
- 블랙잭 결과 상자가 화면 밖으로 밀리던 것 — `카지노 목록으로`(위 막대 `나가기`와 중복)를 뺌
- 바카라 베팅 자리를 펠트 안으로 옮김. 반원 곡선에 잘려서 줄 폭을 256 → 214로 좁힘
- 배포 워크플로가 `App.tsx`의 탭바 한 줄을 문자열로 바꾸던 것을 없앰(그 줄을 고치면
  빌드가 멈췄습니다). 웹 전용 위치 지정은 이제 소스 안에 있습니다

---

## 미해결

**아이폰에서 하단 바가 다시 올라옴** — 전체화면으로 쓰다가 다른 앱을 보고 돌아오면 탭바 아래에
빈 띠가 생깁니다. 지금 처리는 여백을 없앤 게 아니라 **같은 색으로 가린 것**입니다
(`public/index.html`의 배경색). 실기기에서만 재현되어 여기서 검증할 수 없습니다.

---

## 작업 규칙

- **모르면 묻습니다. 정해 준 범위 밖으로 나가지 않습니다.** 여러 대상을 말했을 때
  어느 것인지 확인하고 시작합니다
- 카테고리마다 게임 6개를 넘지 않습니다
- **목록에서 뺀 게임의 코드는 지우지 않습니다.** `gameCategories`에서만 뺍니다.
  한 줄 넣으면 되살아납니다
- 판정 로직은 반드시 `src/*.ts`에 둡니다. 테스트는 `.ts`만 읽을 수 있어 JSX가 든 `.tsx`는 못 읽습니다
- 새 게임을 만들면 `package.json`의 `test` 스크립트에 테스트 파일을 **직접 추가**해야 합니다
- **목록에만 넣고 화면을 안 만들면 시작할 때 오류 화면이 뜹니다**
- 배당은 **짐작하지 말고 시뮬레이션으로 재서** 정합니다. 잰 값은 코드 주석에 남깁니다
- **이겼는데 손해 보는 배당을 두지 않습니다**(1배 미만). 예측 마켓에서 한 번 터진 적 있습니다
- 다 하지 않고 "완성했다"고 말하지 말 것

### 이 PC에서 작업할 때

- Node는 winget으로 설치한 24.19.0인데 셸 PATH에 안 잡힙니다.
  명령 앞에 `export PATH="/c/Program Files/nodejs:$PATH"`를 붙이세요
- `node_modules`는 pnpm 설치본입니다. `npm install`은 이 저장소에서 실패하니, 도구가 필요하면
  저장소 밖 폴더에 따로 설치해 경로로 부르세요(`scripts/trim-hwatu.mjs`가 그 방식입니다)
- **커밋 전에 `git fetch origin`으로 원격을 확인하세요.** GitHub 웹에서 직접 올리는 경우가 있습니다
- 셸에서 백틱이 든 문자열은 먹힙니다. 여러 줄을 고칠 때는 스크래치 폴더에 `.mjs` 파일을 쓰고
  `node`로 돌리는 편이 안전합니다

## 문제 데이터 갱신 (예측 마켓)

`.github/workflows/fetch-kalshi.yml`이 매일 한국 시간 아침 6시에 kalshi.com에서 받아
`src/predictdata.ts`로 커밋합니다. 수동 실행도 됩니다. 앱은 그 파일만 읽습니다
(kalshi가 CORS를 안 열어 브라우저에서 직접 못 부릅니다).

## 목록에서 빠졌지만 코드가 살아 있는 게임

한 줄만 넣으면 되살아납니다.

| 게임 | 코드 |
| --- | --- |
| 육백 | `src/yukbaek.ts` |
| 자동차 레이스 | `src/carracing.ts` |
| 소싸움 | `src/bullfighting.ts` |
| 홀짝 | `oddEvenSetup`/`oddEvenGame` |
| 파이브 카드 드로우 | `src/fivecarddraw.ts` |
| 민화투 | `src/minhwatu.ts` |
| 로또 | `src/worldgames.ts` |
| 즉석 복권 | `ScratchGameScreen` |
| 경정 | `src/boatracing.ts` |
| 코인 푸셔 | `src/coinpusher.ts` |
| 행운의 물고기 | `src/luckyfish.ts` |

## 손 안 댄 것

**`README.md`가 오래됐습니다.** 한국 전통이 "준비 중"이라고 되어 있고, 목록에서 빠진 게임이
그대로 있습니다. 게임 작업이 끝난 뒤 한 번에 고치는 편이 낫습니다.
