# 다음에 할 일 (2026-08-28)

저장소: https://github.com/babodabin/casino · 배포: https://babodabin.github.io/casino
main에 푸시하면 GitHub Actions가 자동 배포합니다. **푸시 = 라이브 배포**입니다.

## 작업 순서

정한 순서입니다. 위에서부터 하나씩 합니다.

1. ~~정리~~ (이 문서)
2. ~~빅투~~ — 로또 자리 · 세계 게임 (완료)
3. ~~코인 푸셔~~ — **접었습니다.** 그레이하운드를 되돌렸습니다
4. ~~스포츠 / 사회문제~~ — **예측 마켓**으로 완성 (경정 자리)
5. **조커 포커(발라트로)** ← 지금 여기 — 즉석 복권 자리 · 세계 게임
6. **낚시** — 행운의 물고기 자리 · 스포츠/아케이드

그다음에 움직임 다듬기를 합니다.

## 지금 상태

- 마지막 커밋 `f7059d1`
- 게임 **34개**, 전부 플레이 가능
- 테스트 **540개 통과**, 타입 오류 0, 웹 빌드 확인됨

| 카테고리 | 게임 (6개 상한) |
| --- | --- |
| 한국 전통 | 고스톱 · 맞고 · 섰다 · 도리짓고땡 · **투전** · 윷 베팅 |
| 카지노 | 블랙잭 · 바카라 · 룰렛 · 크랩스 · 식보 · 슬롯 |
| 포커·카드 | 텍사스 홀덤 · 오마하 · 세븐 포커 · **차이니즈 포커** · 비디오 포커 · 하이로우 |
| 마작 | 리치 · 중국식 · 홍콩 · 사천 (4개, 그대로 두기로 함) |
| 스포츠/아케이드 | 경마 · 경륜 · **예측 마켓** · 그레이하운드 · 피시 레이스 · ~~행운의 물고기~~ |
| 세계 게임 | 야찌 · 파이 고우 · 틴 파티 · **빅투** · ~~즉석 복권~~ · 공 어디에? |

취소선은 새 게임이 완성되면 자리를 넘겨줄 것들입니다. **새 게임이 완성될 때 짝이 되는 옛 게임을 같이 뺍니다.** 먼저 다 빼버리면 카테고리가 휑해지고 그 상태로 배포됩니다.

## 각 게임에서 정해진 것 · 정할 것

### 2. 빅투 (완료)

- 판정 모듈은 **두 명에서 네 명까지** 받습니다. 화면은 지금 네 명 고정이고, **상대 수를 고르는 버튼만 붙이면 됩니다.**
- 배당은 인원수마다 다릅니다. 2명 1.6배 · 3명 2.3배 · 4명 2.95배, 져도 세 장 이하면 절반 회수.

### 3. 코인 푸셔 — 접음

- 두 번 다시 만들었지만 원하는 만큼 나오지 않아 접었습니다. 그레이하운드가 자리로 돌아갔습니다.
- 코드(`src/coinpusher.ts`)와 화면·테스트는 남아 있어 한 줄이면 되살아납니다.

### 4. 예측 마켓 (완료)

- kalshi.com에서 이미 결과가 나온 마켓을 받아 예·아니오를 맞히는 게임입니다. 스포츠와 사회문제 두 갈래.
- **앱이 kalshi를 직접 못 부릅니다(CORS).** 이 하루 한 번(한국 시간 아침 6시) 받아 로 커밋하고, 앱은 그 파일만 읽습니다. 수동 실행도 됩니다.
- 배당은 그 일이 끝나기 전 시장 값에서 뽑습니다(캔들스틱). 어느 쪽에 걸어도 환급률 94%.
- 쓸 수 있는 확률은 0.08~0.92입니다. 밖으로 나가면 배당이 1배 아래가 되어 맞혀도 손해입니다.
- ⚠️ **사회문제 제목은 영어 원문 그대로입니다.** 스포츠만 규칙으로 우리말로 바꿉니다.
- ⚠️ **정답이 공개 저장소에 그대로 들어갑니다.** 파일을 열면 답이 보입니다.

### 5. 조커 포커(발라트로) (즉석 복권 자리)

- 조커 몇 종류, 배수 쌓는 방식, 판 수를 정해야 합니다.
- 이름은 다른 것으로 하고 괄호에 `(발라트로)`를 넣기로 했습니다. **공개 저장소라 상표 이름은 그대로 남는다**는 것을 알고 내린 결정입니다.

### 6. 낚시 (행운의 물고기 자리)

행운의 물고기를 낚시 게임으로 바꿉니다. **둘 중 하나를 골라야 합니다.**

- **낚시 아케이드** — 오락실 물고기 사격 게임. 물고기마다 배당이 다르고 쏴서 맞히면 받습니다. 카지노 게임에 가장 가깝지만 폰에서 조준·연사 조작이 번거롭습니다
- **스크린낚시** — 던지고, 입질을 기다리고, 챔질 타이밍을 맞추고, 릴 싸움을 합니다. 잡은 물고기 종류·크기로 배당. 조작이 타이밍 한두 번이라 폰에 맞습니다

지금 자리에 있는 행운의 물고기(`src/luckyfish.ts`)는 지우지 않고 목록에서만 뺍니다.

## 움직임 다듬기

게임 작업이 끝난 뒤에 합니다.

| 게임 | 할 일 |
| --- | --- |
| 윷 베팅 | 윷가락 던지는 느낌. 지금은 제자리에서 면만 바뀝니다 |
| 비디오 포커 | 카드가 슬롯처럼 돌아가는 느낌 |
| 피시 레이스 | 물고기가 해초에 걸려 자빠지거나 멈칫하는 모습. 사건은 이미 판정에 있는데(`src/fishrace.ts`) 화면에는 글자로만 나옵니다 |

행운의 물고기의 "동그란 곳"은 그 게임이 낚시로 바뀌므로 없어졌습니다.

## 미해결

**아이폰에서 하단 바가 다시 올라옴** — 전체화면으로 쓰다가 다른 앱을 보고 돌아오면 탭바 아래에 빈 띠가 생깁니다. 지금 처리는 여백을 없앤 게 아니라 **같은 색으로 가린 것**입니다 (`public/index.html`의 배경색 + 배포 워크플로의 문자열 치환). 실기기에서만 재현되어 여기서 검증할 수 없습니다. 나중에 확인해 보기로 함.

## 작업 규칙

- **카테고리마다 6개를 넘지 않습니다.**
- **목록에서 뺀 게임의 코드는 지우지 않습니다.** 화면·판정·테스트를 남겨두고 `gameCategories`에서만 뺍니다. 한 줄 넣으면 되살아납니다.
- 판정 로직은 반드시 `src/*.ts`에 둡니다. 테스트는 `node --experimental-strip-types`로 `.ts`만 읽을 수 있어 JSX가 든 `.tsx`는 못 읽습니다.
- 새 게임을 만들면 `package.json`의 `test` 스크립트에 테스트 파일을 **직접 추가**해야 합니다.
- **목록에만 넣고 화면 컴포넌트를 안 만들면 시작할 때 오류 화면이 뜹니다.** 실제로 그렇게 배포된 적이 있습니다.
- 웹에서 클라이언트가 GitHub에 데이터를 쓰지 않습니다. 공개 저장소라 토큰이 노출되고 저장할 때마다 재배포됩니다. 데이터 백업은 JSON 내보내기·불러오기(`src/backup.ts`).
- 배포 워크플로가 `App.tsx`에서 탭바 한 줄을 문자열로 정확히 1개 찾아 바꿉니다. **못 찾으면 빌드가 실패합니다.**
- 다 하지 않고 "완성했다"고 말하지 말 것. 빠뜨린 게 있으면 먼저 말할 것.

### 이 PC에서 작업할 때

- Node는 winget으로 설치한 24.19.0인데 셸 PATH에 안 잡힙니다. 명령 앞에 `export PATH="/c/Program Files/nodejs:$PATH"`를 붙이세요.
- `node_modules`는 pnpm 설치본이라 최상위에 직접 의존성만 링크로 있습니다(정상).
- **커밋 전에 `git fetch origin`으로 원격을 확인하세요.** GitHub 웹에서 직접 파일을 올리는 경우가 있어, 로컬 작업분이 이미 원격에 올라가 있을 수 있습니다.

## 목록에서 빠졌지만 코드가 살아 있는 게임

한 줄만 넣으면 되살아납니다.

| 게임 | 코드 |
| --- | --- |
| 육백 | `src/yukbaek.ts`, `yukbaekSetup`/`yukbaekGame` |
| 자동차 레이스 | `src/carracing.ts`, `carSetup`/`carGame` |
| 소싸움 | `src/bullfighting.ts`, `bullSetup`/`bullGame` |
| 홀짝 | `oddEvenSetup`/`oddEvenGame` |
| 파이브 카드 드로우 | `src/fivecarddraw.ts`, `fiveDrawSetup`/`fiveDrawGame` |
| 민화투 | `src/minhwatu.ts`, `minhwatuSetup`/`minhwatuGame` |

로또(`src/worldgames.ts`)와 코인 푸셔(`src/coinpusher.ts`)도 여기에 들어왔습니다. 앞으로 경정 · 즉석 복권 · 행운의 물고기가 더 들어옵니다.

## 손 안 댄 것

**`README.md`가 오래됐습니다.** 한국 전통이 "준비 중"이라고 되어 있고, 목록에서 빠진 파이브 카드 드로우가 그대로 있고, 스포츠·세계 게임 카테고리가 없는 것으로 적혀 있습니다. 게임 작업이 끝난 뒤 한 번에 고치는 편이 낫습니다.
